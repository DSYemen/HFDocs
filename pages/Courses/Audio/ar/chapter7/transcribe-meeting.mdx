# نسخ محتوى اجتماع

في هذا القسم الأخير، سنستخدم نموذج Whisper لتوليد نسخة لحديث أو اجتماع بين متحدثين أو أكثر. بعد ذلك، سنقرنه بنموذج *تمييز المتحدث* للتنبؤ بـ "من تحدث ومتى". من خلال مطابقة الطوابع الزمنية من نسخ Whisper مع الطوابع الزمنية من نموذج تمييز المتحدث، يمكننا التنبؤ بنسخة نهاية إلى نهاية من الاجتماع مع أوقات بداية/نهاية كاملة التنسيق لكل متحدث. هذه هي النسخة الأساسية من خدمات نسخ الاجتماعات التي قد تكون شاهدتها عبر الإنترنت من شركات مثل [Otter.ai](https://otter.ai) وغيرها:

## تمييز المتحدث

تمييز المتحدث (أو diarisation) هي مهمة أخذ إدخال صوتي غير معنون والتنبؤ بـ "من تحدث ومتى". وبهذه الطريقة، يمكننا التنبؤ ببداية/نهاية الطوابع الزمنية لكل متحدث، مما يتوافق مع وقت بدء كل متحدث في الكلام ووقت انتهائه.

🤗 المحولات لا تحتوي حاليًا على نموذج لتمييز المتحدثين مدرج في المكتبة، ولكن هناك نقاط مرجعية على Hub يمكن استخدامها بسهولة نسبية. في هذا المثال، سنستخدم نموذج تمييز المتحدث المعلم مسبقًا من [pyannote.audio](https://github.com/pyannote/pyannote-audio). دعونا نبدأ ونثبت حزمة:

```bash
pip install --upgrade pyannote.audio
```

رائع! يتم استضافة أوزان هذا النموذج على Hub من Hugging Face. للوصول إليها، يجب أولاً أن نوافق على شروط استخدام نموذج تمييز المتحدث: [pyannote/speaker-diarization](https://huggingface.co/pyannote/speaker-diarization). وبعد ذلك شروط استخدام نموذج التجزئة: [pyannote/segmentation](https://huggingface.co/pyannote/segmentation).

بمجرد الانتهاء، يمكننا تحميل خط أنابيب تمييز المتحدث المعلم مسبقًا محليًا على جهازنا:

```python
from pyannote.audio import Pipeline

diarization_pipeline = Pipeline.from_pretrained(
"pyannote/speaker-diarization@2.1", use_auth_token=True
)
```

دعونا نجربها على ملف صوتي عينة! لهذا، سنقوم بتحميل عينة من مجموعة بيانات [LibriSpeech ASR](https://huggingface.co/datasets/librispeech_asr)
التي تتكون من متحدثين مختلفين تم ضمها معًا لإعطاء ملف صوتي واحد:

```python
from datasets import load_dataset

concatenated_librispeech = load_dataset(
"sanchit-gandhi/concatenated_librispeech", split="train", streaming=True
)
sample = next(iter(concatenated_librispeech))
```

يمكننا الاستماع إلى الصوت لمعرفة كيف يبدو:

```python
from IPython.display import Audio

Audio(sample["audio"]["array"], rate=sample["audio"]["sampling_rate"])
```

رائع! يمكننا بوضوح سماع متحدثين مختلفين، مع انتقال حوالي 15 ثانية من الطريق. دعونا نمرر هذا الملف الصوتي إلى نموذج التجزئة للحصول على أوقات بداية/نهاية المتحدث. لاحظ أن pyannote.audio يتوقع أن يكون الإدخال الصوتي عبارة عن Tensor PyTorch من الشكل `(القنوات، طول_المسلسل)`، لذلك نحتاج إلى إجراء هذا التحويل قبل تشغيل النموذج:

```python
import torch

input_tensor = torch.from_numpy(sample["audio"]["array"][None, :]).float()
outputs = diarization_pipeline(
{"waveform": input_tensor, "sample_rate": sample["audio"]["sampling_rate"]}
)

outputs.for_json()["content"]
```

```text
[{'segment': {'start': 0.4978125، 'end': 14.520937500000002}،
'track': 'B'،
'label': 'SPEAKER_01'}،
{'segment': {'start': 15.364687500000002، 'end': 21.3721875}،
'track': 'A'،
'label': 'SPEAKER_00'}]
```

يبدو هذا جيدًا! يمكننا أن نرى أن المتحدث الأول يتنبأ بالتحدث حتى علامة 14.5 ثانية، والمتحدث الثاني من 15.4 ثانية فصاعدًا. الآن نحتاج إلى الحصول على النسخ النصي الخاص بنا!

## نسخ الكلام

للمرة الثالثة في هذه الوحدة، سنستخدم نموذج Whisper لنظام نسخ الكلام الخاص بنا. على وجه التحديد، سنقوم بتحميل
[نقطة تفتيش Whisper Base](https://huggingface.co/openai/whisper-base)، نظرًا لأنه صغير بما يكفي لمنح
سرعة استدلال جيدة مع دقة نسخ معقولة. كما هو الحال دائمًا، لا تتردد في استخدام أي نقطة تفتيش للتعرف على الكلام
[على Hub](https://huggingface.co/models?pipeline_tag=automatic-speech-recognition&library=transformers&sort=trending)،
بما في ذلك Wav2Vec2، MMS ASR أو نقاط تفتيش Whisper الأخرى:

```python
from transformers import pipeline

asr_pipeline = pipeline(
"automatic-speech-recognition"،
model="openai/whisper-base"،
)
```

دعونا نحصل على النسخ النصي لعينتنا الصوتية، مع إعادة أوقات المقاطع الزمنية حتى نعرف
بداية/نهاية الأوقات لكل مقطع. ستتذكر من الوحدة 5 أننا نحتاج إلى تمرير الحجة
`return_timestamps=True` لتنشيط مهمة التنبؤ بالطابع الزمني لـ Whisper:

```python
asr_pipeline(
sample["audio"].copy()،
generate_kwargs={"max_new_tokens": 256}،
return_timestamps=True،
)
```

```text
{
"النص": "النقطة الثانية والأهمية هي كما يلي. يمكن تعريف السيادة بأنها الحق في صنع القوانين. في فرنسا، يمارس الملك حقًا جزءًا من السلطة السيادية، حيث لا تحمل القوانين أي وزن. كان في حالة مزاجية مفضلة، بسبب الدمار الذي هددت به فعل زوجته بإلقاء ظلالها على مستقبله بالكامل."،
"المقاطع": [
{"الطابع الزمني": (0.0، 3.56)، "النص": "النقطة الثانية والأهمية هي كما يلي."}،
{
"الطابع الزمني": (3.56، 7.84)،
"النص": "يمكن تعريف السيادة بأنها الحق في صنع القوانين."
}،
{
"الطابع الزمني": (7.84، 13.88)،
"النص": "في فرنسا، يمارس الملك حقًا جزءًا من السلطة السيادية، حيث لا تحمل القوانين أي وزن."
}،
{"الطابع الزمني": (13.88، 15.48)، "النص": "لا يوجد وزن."}،
{
"الطابع الزمني": (15.48، 19.44)،
"النص": "كان في حالة مزاجية مفضلة، بسبب الدمار الذي هددت به فعل زوجته بإلقاء ظلالها على مستقبله بالكامل."
}،
{"الطابع الزمني": (19.44، 21.28)، "النص": "،"}
]
}
```

حسنا! نرى أن لكل مقطع من النسخة النصية وقت بداية ونهاية، مع تغيير المتحدثين عند علامة 15.48 ثانية. الآن يمكننا أن نقرن هذه النسخة النصية بالطوابع الزمنية للمتحدث التي حصلنا عليها من نموذج التجزئة لدينا للحصول على النسخة النصية النهائية.

## صندوق الكلام

للحصول على النسخة النصية النهائية، سنقوم بمحاذاة الطوابع الزمنية من نموذج التجزئة مع تلك الموجودة في نموذج Whisper.
تنبأ نموذج التجزئة بأن المتحدث الأول ينتهي عند 14.5 ثانية، ويبدأ المتحدث الثاني عند 15.4 ثانية، في حين تنبأ Whisper بحدود المقاطع عند
13.88، 15.48 و19.44 ثانية على التوالي. نظرًا لأن الطوابع الزمنية من Whisper لا تتطابق تمامًا مع تلك الموجودة في
نموذج التجزئة، نحتاج إلى العثور على أي من هذه الحدود أقرب إلى 14.5 و15.4 ثانية، وقسم النسخة النصية حسب المتحدثين وفقًا لذلك. على وجه التحديد، سنبحث عن أقرب محاذاة بين الطوابع الزمنية للتجزئة والنسخ النصي عن طريق
تقليل المسافة المطلقة بين الاثنين.

لحسن الحظ، يمكننا استخدام حزمة 🤗 Speechbox لأداء هذا المحاذاة. أولاً، دعونا نثبت `speechbox` من
الرئيسية:

```bash
pip install git+https://github.com/huggingface/speechbox
```

الآن يمكننا إنشاء خط أنابيب التجزئة والنسخ النصي المركب، عن طريق تمرير نموذج التجزئة ونموذج
ASR إلى فئة [`ASRDiarizationPipeline`](https://github.com/huggingface/speechbox/tree/main#asr-with-speaker-diarization):

```python
from speechbox import ASRDiarizationPipeline

pipeline = ASRDiarizationPipeline(
asr_pipeline=asr_pipeline، diarization_pipeline=diarization_pipeline
)
```

<Tip>
يمكنك أيضًا إنشاء <code>ASRDiarizationPipeline</code> مباشرة من المعلم عن طريق تحديد معرف النموذج
من نقطة تفتيش ASR على Hub:
<p><code>pipeline = ASRDiarizationPipeline.from_pretrained("openai/whisper-base")</code></p>
</Tip>

دعونا نمرر الملف الصوتي إلى خط الأنابيب المركب ونرى ما الذي نحصل عليه:

```python
pipeline(sample["audio"].copy())
```

```text
[{'speaker': 'SPEAKER_01'،
'النص': 'النقطة الثانية والأهمية هي كما يلي. يمكن تعريف السيادة بأنها الحق في صنع القوانين. في فرنسا، يمارس الملك حقًا جزءًا من السلطة السيادية، حيث لا تحمل القوانين أي وزن.'،
'الطابع الزمني': (0.0، 15.48)}،
{'speaker': 'SPEAKER_00'،
'النص': "كان في حالة مزاجية مفضلة، بسبب الدمار الذي هددت به فعل زوجته بإلقاء ظلالها على مستقبله بالكامل."،
'الطابع الزمني': (15.48، 21.28)}]
```

ممتاز! يتم تجزئة المتحدث الأول على أنه يتحدث من 0 إلى 15.48 ثانية، والمتحدث الثاني من 15.48 إلى 21.28 ثانية،
مع النسخ النصية المقابلة لكل منهما.

يمكننا تنسيق الطوابع الزمنية بشكل أفضل قليلاً عن طريق تحديد دالتين مساعدتين. الأول يحول زوجًا من
الطوابع الزمنية إلى سلسلة، مقربة إلى عدد محدد من المنازل العشرية. يجمع الثاني معرف المتحدث والطابع الزمني ومعلومات النص
في سطر واحد، ويقسم كل متحدث إلى سطر خاص به للسهولة في القراءة:

```python
def tuple_to_string(start_end_tuple، ndigits=1):
return str((round(start_end_tuple[0]، ndigits)، round(start_end_tuple[1]، ndigits)))


def format_as_transcription(raw_segments):
return "\n\n".join(
[
chunk ["المتحدث"] + " " + tuple_to_string(chunk ["الطابع الزمني"]) + chunk ["النص"]
for chunk in raw_segments
]
)
```

دعونا نعيد تشغيل خط الأنابيب، هذه المرة تنسيق النسخة النصية وفقًا للدالة التي حددناها للتو:

```python
outputs = pipeline(sample["audio"].copy())

format_as_transcription(outputs)
```

```text
SPEAKER_01 (0.0، 15.5) النقطة الثانية والأهمية هي كما يلي. يمكن تعريف السيادة بأنها الحق في صنع القوانين.
في فرنسا، يمارس الملك حقًا جزءًا من السلطة السيادية، حيث لا تحمل القوانين أي وزن.

SPEAKER_00 (15.5، 21.3) كان في حالة مزاجية مفضلة، بسبب الدمار الذي هددت به فعل زوجته بإلقاء ظلالها على مستقبله بالكامل.
```

ها نحن ذا! بهذا، قمنا بتجزئة نسخة صوت الإدخال الخاص بنا وحصلنا على نسخ منقسمة إلى متحدثين.
في حين أن خوارزمية المسافة الدنيا لمحاذاة الطوابع الزمنية المجزأة والطوابع الزمنية المنقولة بسيطة،
يعمل بشكل جيد في الممارسة العملية. إذا كنت ترغب في استكشاف طرق أكثر تقدمًا لدمج الطوابع الزمنية،
رمز المصدر لـ `ASRDiarizationPipeline` هو مكان جيد للبدء: [speechbox/diarize.py](https://github.com/huggingface/speechbox/blob/96d2d1a180252d92263f862a1cd25a48860f1aed/src/speechbox/diarize.py#L12)