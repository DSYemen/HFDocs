# إنشاء مساعد صوتي

في هذا القسم، سنقوم بجمع ثلاثة نماذج لدينا بالفعل خبرة عملية معها لبناء مساعد صوتي شامل يسمى **مارفن** 🤖. مثل "أليكسا" من أمازون أو "سيري" من أبل، مارفن هو مساعد صوتي افتراضي يستجيب لـ "كلمة تنبيه" معينة، ثم يستمع إلى استعلام منطوق، وأخيرا يستجيب بإجابة منطوقة.

يمكننا تقسيم خط أنابيب المساعد الصوتي إلى أربع مراحل، تتطلب كل منها نموذجًا مستقلًا:

### 1. اكتشاف كلمة التنبيه

يستمع المساعدون الصوتيون باستمرار إلى إدخالات الصوت القادمة عبر ميكروفون جهازك، ولكنهم لا يبدأون العمل إلا عند نطق "كلمة تنبيه" أو "كلمة تشغيل" معينة.

تتم معالجة مهمة اكتشاف كلمة التنبيه بواسطة نموذج تصنيف صوتي صغير على الجهاز، وهو أصغر وأخف بكثير من نموذج التعرف على الكلام، وغالبًا ما يكون به عدة ملايين من المعلمات مقارنة بعدة مئات من الملايين للتعرف على الكلام. وبالتالي، يمكن تشغيله باستمرار على جهازك دون استنزاف بطاريتك. فقط عندما يتم اكتشاف كلمة التنبيه يتم تشغيل نموذج التعرف على الكلام الأكبر، وبعد ذلك يتم إيقاف تشغيله مرة أخرى.

### 2. نسخ الكلام

المرحلة التالية في خط الأنابيب هي نسخ الاستعلام المنطوق إلى نص. من الناحية العملية، فإن نقل ملفات الصوت من جهازك المحلي إلى السحابة بطيء بسبب الطبيعة الكبيرة لملفات الصوت، لذلك من الأكثر كفاءة نسخها مباشرة باستخدام نموذج التعرف التلقائي على الكلام (ASR) على الجهاز بدلاً من استخدام نموذج في السحابة. قد يكون النموذج الموجود على الجهاز أصغر وبالتالي أقل دقة من النموذج المستضاف في السحابة، ولكن سرعة الاستدلال الأسرع تجعله مجديًا حيث يمكننا تشغيل التعرف على الكلام في الوقت الفعلي تقريبًا، حيث يتم نسخ نصنا المنطوق أثناء التحدث به.

نحن على دراية جيدة الآن بعملية التعرف على الكلام، لذا يجب أن يكون هذا سهلاً!

### 3. استعلام نموذج اللغة

الآن بعد أن عرفنا ما طلبه المستخدم، نحتاج إلى توليد استجابة! أفضل النماذج المرشحة لهذه المهمة هي *نماذج اللغة الكبيرة (LLMs)*، حيث يمكنها فهم دلالة استعلام النص وتوليد استجابة مناسبة.

نظرًا لأن استعلام النص الخاص بنا صغير (مجرد عدد قليل من رموز النص)، والنماذج اللغوية كبيرة (العديد من المليارات من المعلمات)، فإن أكثر الطرق كفاءة لتشغيل استدلال LLM هي إرسال استعلام النص الخاص بنا من جهازنا إلى LLM يعمل في السحابة، وتوليد استجابة نصية، وإعادة الاستجابة إلى الجهاز.

### 4. تخليق الكلام

أخيرًا، سنستخدم نموذج تحويل النص إلى كلام (TTS) لتخليق الاستجابة النصية ككلام منطوق. يتم ذلك على الجهاز، ولكن يمكنك تشغيل نموذج TTS في السحابة بشكل عملي، وتوليد الإخراج الصوتي وإعادته إلى الجهاز.

مرة أخرى، لقد قمنا بذلك عدة مرات الآن، لذا ستكون العملية مألوفة جدًا!

<Tip>
يتطلب القسم التالي استخدام ميكروفون لتسجيل إدخال صوتي. نظرًا لأن أجهزة Google Colab لا تتوافق مع الميكروفون، يوصى بتشغيل هذا القسم محليًا، إما على وحدة المعالجة المركزية الخاصة بك، أو على وحدة معالجة الرسومات إذا كان لديك حق الوصول المحلي. تم اختيار أحجام نقاط التفتيش لتكون صغيرة بدرجة كافية للعمل بسرعة كافية على وحدة المعالجة المركزية، لذا فستحصل على أداء جيد حتى بدون وحدة معالجة الرسومات.
</Tip>

## اكتشاف كلمة التنبيه

المرحلة الأولى في خط أنابيب المساعد الصوتي هي اكتشاف ما إذا كانت كلمة التنبيه قد نطقت، وعلينا أن نجد نموذجًا مسبق التدريب مناسبًا لهذه المهمة! ستتذكر من القسم الخاص [بالنماذج المسبقة التدريب لتصنيف الصوت](../chapter4/classification_models) أن [الأوامر الصوتية](https://huggingface.co/datasets/speech_commands) هي مجموعة بيانات من الكلمات المنطوقة المصممة لتقييم نماذج تصنيف الصوت على 15+ كلمة أمر بسيطة مثل `"up"`، `"down"`، `"yes"` و `"no"`، بالإضافة إلى تسمية `"silence"` لتصنيف عدم وجود كلام. خذ دقيقة للاستماع إلى العينات في عارض المجموعات البيانات على المركز وتعرف على مجموعة بيانات الأوامر الصوتية: [عارض المجموعات](https://huggingface.co/datasets/speech_commands/viewer/v0.01/train).

يمكننا أن نأخذ نموذج تصنيف صوتي مدرب مسبقًا على مجموعة بيانات الأوامر الصوتية واختيار إحدى كلمات الأوامر البسيطة هذه لتكون كلمة التنبيه المختارة. من بين 15+ كلمة أمر محتملة، إذا تنبأ النموذج بكلمة التنبيه التي اخترناها بأعلى احتمال، فيمكننا التأكد إلى حد ما من نطق كلمة التنبيه.

فلنتوجه إلى Hugging Face Hub وانقر فوق علامة التبويب "النماذج": https://huggingface.co/models

سيؤدي هذا إلى عرض جميع النماذج على Hugging Face Hub، مرتبة حسب التنزيلات في آخر 30 يومًا:

ستلاحظ على الجانب الأيسر أن لدينا مجموعة من علامات التبويب التي يمكننا تحديدها لتصفية النماذج حسب المهمة أو المكتبة أو مجموعة البيانات، إلخ. قم بالتمرير لأسفل وحدد المهمة "تصنيف الصوت" من قائمة المهام الصوتية:

الآن يتم تقديمنا بمجموعة فرعية من 500+ نموذج تصنيف صوتي على المركز. لزيادة تنقيح هذا الاختيار، يمكننا تصفية النماذج حسب مجموعة البيانات. انقر فوق علامة التبويب "مجموعات البيانات"، وفي مربع البحث اكتب "speech_commands". أثناء الكتابة، سترى خيار `speech_commands` يظهر أسفل علامة التبويب "البحث". يمكنك النقر فوق هذا الزر لتصفية جميع نماذج تصنيف الصوت إلى تلك التي تمت تهيئتها الدقيقة على مجموعة بيانات الأوامر الصوتية:

رائع! نرى أن لدينا ستة نماذج مدربة مسبقًا متاحة لهذه المجموعة من البيانات والمهمة المحددة (على الرغم من أنه قد تكون هناك نماذج جديدة تمت إضافتها إذا كنت تقرأ في تاريخ لاحق!). ستتعرف على النموذج الأول من هذه النماذج باعتباره [نقطة تفتيش محول طيف الصوت](https://huggingface.co/MIT/ast-finetuned-speech-commands-v2) التي استخدمناها في مثال الوحدة 4. سنستخدم نقطة التفتيش هذه مرة أخرى لمهمة اكتشاف كلمة التنبيه.

فلنمض قدمًا وقم بتحميل نقطة التفتيش باستخدام فئة `pipeline`:

```python
from transformers import pipeline
import torch

device = "cuda:0" if torch.cuda.is_available() else "cpu"

classifier = pipeline(
"audio-classification", model="MIT/ast-finetuned-speech-commands-v2", device=device
)
```

يمكننا التحقق من التصنيفات التي تم تدريب النموذج عليها عن طريق التحقق من سمة `id2label` في تكوين النموذج:

```python
classifier.model.config.id2label
```

حسنًا! نرى أن النموذج تم تدريبه على 35 تسمية فئة، بما في ذلك بعض كلمات الأوامر البسيطة التي وصفناها أعلاه، بالإضافة إلى بعض الأشياء المحددة مثل `"bed"` و `"house"` و `"cat"`. نرى أن هناك اسمًا واحدًا في تسميات الفئات هذه: يرمز الرقم 27 إلى التسمية **"marvin"**:

```python
classifier.model.config.id2label[27]
```

```
'marvin'
```

ممتاز! يمكننا استخدام هذا الاسم ككلمة تنبيه لمساعدنا الصوتي، على غرار كيفية استخدام "Alexa" لمساعد Alexa من Amazon، أو "Hey Siri" لمساعد Siri من Apple. من بين جميع التصنيفات المحتملة، إذا تنبأ النموذج بـ `"marvin"` بأعلى احتمال للصنف، فيمكننا التأكد إلى حد ما من نطق كلمة التنبيه التي اخترناها.

الآن نحتاج إلى تحديد دالة تستمع باستمرار إلى إدخال ميكروفون الجهاز، وتمرر الصوت باستمرار إلى نموذج التصنيف للاستدلال. للقيام بذلك، سنستخدم دالة مساعدة مفيدة تأتي مع 🤗 Transformers تسمى [`ffmpeg_microphone_live`](https://github.com/huggingface/transformers/blob/fb78769b9c053876ed7ae152ee995b0439a4462a/src/transformers/pipelines/audio_utils.py#L98).

تعيد هذه الدالة كائنًا *مولدًا*، مما يؤدي إلى إنتاج تسلسل من قطع الصوت التي يمكن تمرير كل منها إلى نموذج التصنيف لإجراء تنبؤ. يمكننا تمرير هذا المولد مباشرة إلى `pipeline`، والذي بدوره يعيد تسلسل التنبؤات، واحد لكل قطعة صوت إدخال. يمكننا فحص احتمالات تسمية الفئة لكل قطعة صوت، وإيقاف حلقة اكتشاف كلمة التنبيه عندما نكتشف أنه تم نطق كلمة التنبيه.

سنستخدم معايير بسيطة جدًا لتصنيف ما إذا كانت كلمة التنبيه الخاصة بنا قد نطقت: إذا كانت تسمية الفئة ذات أعلى احتمال هي كلمة التنبيه الخاصة بنا، وكان هذا الاحتمال يتجاوز عتبة `prob_threshold`، فإننا نعلن أن كلمة التنبيه قد نطقت. إن استخدام عتبة احتمالية لبوابة مصنفنا بهذه الطريقة يضمن عدم التنبؤ بكلمة التنبيه بشكل خاطئ إذا كان إدخال الصوت ضوضاء، وهو ما يحدث عادةً عندما يكون النموذج غير متأكد جدًا وجميع احتمالات تسمية الفئة منخفضة. قد ترغب في ضبط عتبة الاحتمال هذه، أو استكشاف وسائل أكثر تطورًا لاتخاذ قرار كلمة التنبيه من خلال مقياس يعتمد على [*الإنتروبيا*](https://en.wikipedia.org/wiki/Entropy_(information_theory)) (أو عدم اليقين).

```python
from transformers.pipelines.audio_utils import ffmpeg_microphone_live


def launch_fn(
wake_word="marvin",
prob_threshold=0.5,
chunk_length_s=2.0,
stream_chunk_s=0.25,
debug=False,
):
if wake_word not in classifier.model.config.label2id.keys():
raise ValueError(
f"Wake word {wake_word} not in set of valid class labels, pick a wake word in the set {classifier.model.config.label2id.keys()}."
)

sampling_rate = classifier.feature_extractor.sampling_rate

mic = ffmpeg_microphone_live(
sampling_rate=sampling_rate,
chunk_length_s=chunk_length_
## نظام نسخ النص المكتوب

مرة أخرى، سنستخدم نموذج Whisper لنظام نسخ النص المكتوب الخاص بنا. على وجه التحديد، سنقوم بتحميل نقطة تفتيش [Whisper Base English](https://huggingface.co/openai/whisper-base.en)
، نظرًا لأنها صغيرة بما يكفي لتوفير سرعة استدلال جيدة مع دقة نسخ معقولة. سنستخدم حيلة
للحصول على نسخ نص شبه آني من خلال الذكاء في كيفية إرسال إدخالات الصوت الخاصة بنا إلى النموذج. كما هو الحال من قبل، لا تتردد في استخدام أي نقطة تفتيش للتعرف على الكلام على [Hub](https://huggingface.co/models?pipeline_tag=automatic-speech-recognition&library=transformers&sort=trending)
،
بما في ذلك Wav2Vec2، MMS ASR أو نقاط تفتيش Whisper الأخرى:

```python
transcriber = pipeline(
"automatic-speech-recognition", model="openai/whisper-base.en", device=device
)
```

<Tip>
إذا كنت تستخدم وحدة معالجة الرسوميات (GPU)، فيمكنك زيادة حجم نقطة التفتيش لاستخدام نقطة تفتيش <a href="https://huggingface.co/openai/whisper-small.en">Whisper Small English</a>
، والتي ستعيد دقة نسخ أفضل وستظل ضمن عتبة الكمون المطلوبة. ما عليك سوى استبدال
معرف النموذج إلى: <code>"openai/whisper-small.en"</code>.
</Tip>

الآن يمكننا تحديد دالة لتسجيل إدخال الميكروفون الخاص بنا ونسخ النص المقابل. باستخدام وظيفة المساعد `ffmpeg_microphone_live`
، يمكننا التحكم في مدى "الوقت الفعلي" لنموذج التعرف على الكلام الخاص بنا. إن استخدام `stream_chunk_s` أصغر يسهل التعرف على الكلام في الوقت الفعلي،
حيث نقسم صوت الإدخال الخاص بنا إلى قطع أصغر وننسخها أثناء التنقل. ومع ذلك، يأتي هذا على حساب الدقة الضعيفة، حيث يوجد سياق أقل للنموذج للاستدلال منه.

بينما نقوم بنسخ الكلام، نحتاج أيضًا إلى فكرة حول موعد توقف المستخدم عن التحدث، حتى نتمكن من إنهاء
التسجيل. للبساطة، سننهي تسجيل الميكروفون الخاص بنا بعد أول `chunk_length_s` (والذي تم تعيينه
إلى 5 ثوانٍ بشكل افتراضي)، ولكن يمكنك تجربة استخدام نموذج اكتشاف نشاط الصوت (VAD) للتنبؤ بموعد توقف المستخدم عن التحدث.

```python
import sys


def transcribe(chunk_length_s=5.0, stream_chunk_s=1.0):
sampling_rate = transcriber.feature_extractor.sampling_rate

mic = ffmpeg_microphone_live(
sampling_rate=sampling_rate,
chunk_length_
s=chunk_length_s,
stream_chunk_s=stream_chunk_s,
)

print("Start speaking...")
for item in transcriber(mic, generate_kwargs={"max_new_tokens": 128}):
sys.stdout.write("\033[K")
print(item["text"], end="\r")
if not item["partial"][0]:
break

return item["text"]
```

دعونا نجرب هذا ونرى كيف سنحصل عليه! بمجرد تشغيل الميكروفون، ابدأ في التحدث وشاهد النسخ الخاص بك
يظهر في شبه الوقت الفعلي:

```python
transcribe()
```

```text
Start speaking...
Hey, this is a test with the whisper model.
```

رائع! يمكنك ضبط طول الصوت الأقصى `chunk_length_s` بناءً على مدى سرعة أو بطء حديثك (زد منه إذا
شعرت أنك لم تحصل على وقت كافٍ للتحدث، وانقصه إذا كنت تنتظر في النهاية)، و
`stream_chunk_s` لعامل الوقت الفعلي. ما عليك سوى تمرير هذه الحجج إلى دالة `transcribe`.

## استعلام لغة النماذج

الآن بعد أن حصلنا على استعلام منطوق منسوخ، نريد توليد استجابة ذات معنى. للقيام بذلك، سنستخدم LLM
يتم استضافتها في السحابة. على وجه التحديد، سنختار LLM على Hugging Face Hub واستخدام [Inference API](https://huggingface.co/inference-api)
لاستعلام النموذج بسهولة.

أولاً، دعنا نتوجه إلى Hugging Face Hub. للعثور على LLM الخاص بنا، سنستخدم [🤗 Open LLM Leaderboard](https://huggingface.co/spaces/HuggingFaceH4/open_llm_leaderboard)
،
مساحة تقوم بترتيب نماذج LLM حسب الأداء على أربع مهام للجيل. سنبحث عن طريق "instruct" لتصفية النماذج
التي تم ضبط دقتها باستخدام التعليمات، حيث يجب أن تعمل هذه النماذج بشكل أفضل لمهمة الاستعلام الخاصة بنا:

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/audio-course-images/resolve/main/llm_leaderboard.png">
</div>

سنستخدم نقطة تفتيش [tiiuae/falcon-7b-instruct](https://huggingface.co/tiiuae/falcon-7b-instruct) بواسطة [TII](https://www.tii.ae/)
،
LM فك تشفير فقط بحجم 7 بليون معلمة تم ضبط دقتها باستخدام مزيج من مجموعات البيانات الدردشة والتعليمات. يمكنك استخدام أي LLM على Hugging Face Hub الذي تم تمكين "Hosted inference API" منه، ما عليك سوى البحث عن الأداة الموجودة على الجانب الأيمن من بطاقة النموذج:

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/audio-course-images/resolve/main/inference_api.png">
</div>

تسمح لنا واجهة برمجة التطبيقات الخاصة بالاستدلال بإرسال طلب HTTP من جهاز الكمبيوتر المحلي الخاص بنا إلى LLM المستضاف على Hub، ويعيد
الاستجابة كملف `json`. كل ما نحتاج إلى توفيره هو رمز Hugging Face Hub الخاص بنا (الذي نحصل عليه مباشرة من مجلد Hugging Face
Hub) ومعرف النموذج LLM الذي نريد الاستعلام عنه:

```python
from huggingface_hub import HfFolder
import requests


def query(text, model_id="tiiuae/falcon-7b-instruct"):
api_url = f"https://api-inference.huggingface.co/models/{model_id}"
headers = {"Authorization": f"Bearer {HfFolder().get_token()}"}
payload = {"inputs": text}

print(f"Querying...: {text}")
response = requests.post(api_url, headers=headers, json=payload)
return response.json()[0]["generated_text"][len(text) + 1 :]
```

دعونا نجربها مع إدخال الاختبار!

```python
query("What does Hugging Face do?")
```

```
'Hugging Face is a company that provides natural language processing and machine learning tools for developers. They'
```

ستلاحظ مدى سرعة الاستدلال باستخدام واجهة برمجة تطبيقات الاستدلال - يتعين علينا فقط إرسال عدد صغير من رموز النص
من جهاز الكمبيوتر المحلي الخاص بنا إلى النموذج المستضاف، لذا فإن تكلفة الاتصال منخفضة جدًا. يتم استضافة LLM على وحدات معالجة الرسوميات (GPU)،
لذا فإن الاستدلال سريع للغاية. وأخيرًا، يتم نقل الاستجابة المولدة مرة أخرى من النموذج إلى جهاز الكمبيوتر المحلي الخاص بنا،
مرة أخرى مع انخفاض كبير في الاتصال.

## تخليق الكلام

والآن نحن مستعدون للحصول على الإخراج الصوتي النهائي! مرة أخرى، سنستخدم نموذج Microsoft [SpeechT5 TTS](https://huggingface.co/microsoft/speecht5_tts)
للغة الإنجليزية TTS، ولكن يمكنك استخدام أي نموذج TTS من اختيارك. دعونا ننتقل إلى تحميل المعالج والنموذج:

```python
from transformers import SpeechT5Processor, SpeechT5ForTextToSpeech, SpeechT5HifiGan

processor = SpeechT5Processor.from_pretrained("microsoft/speecht5_tts")

model = SpeechT5ForTextToSpeech.from_pretrained("microsoft/speecht5_tts").to(device)
vocoder = SpeechT5HifiGan.from_pretrained("microsoft/speecht5_hifigan").to(device)
```

وأيضًا تضمين المتحدث:

```python
from datasets import load_dataset

embeddings_dataset = load_dataset("Matthijs/cmu-arctic-xvectors", split="validation")
speaker_embeddings = torch.tensor(embeddings_dataset[7306]["xvector"]).unsqueeze(0)
```

سنعيد استخدام دالة `synthesise` التي حددناها في الفصل السابق حول [الترجمة من الكلام إلى الكلام](speech-to-speech):

```python
def synthesise(text):
inputs = processor(text=text, return_tensors="pt")
speech = model.generate_speech(
inputs["input_ids"].to(device), speaker_embeddings.to(device), vocoder=vocoder
)
return speech.cpu()
```

دعونا نتحقق بسرعة من أن هذا يعمل كما هو متوقع:

```python
from IPython.display import Audio

audio = synthesise(
"Hugging Face is a company that provides natural language processing and machine learning tools for developers."
)

Audio(audio, rate=16000)
```

أداء جيد 👍

## مارفن 🤖

الآن بعد أن حددنا دالة لكل مرحلة من مراحل مساعد الصوت الأربع، كل ما تبقى هو
قطعة معا للحصول على مساعد الصوت الخاص بنا من البداية إلى النهاية. سنقوم ببساطة بتوصيل المراحل الأربع، بدءًا من
الكشف عن كلمة الاستيقاظ (`launch_fn`)، ونسخ الكلام، واستعلام LLM، وأخيرًا تخليق الكلام.

```python
launch_fn()
transcription = transcribe()
response = query(transcription)
audio = synthesise(response)

Audio(audio, rate=16000, autoplay=True)
```

جربها مع بعض المطالبات! فيما يلي بعض الأمثلة للبدء:

* ما هو البلد الأكثر سخونة في العالم؟
* كيف تعمل نماذج المحول؟
* هل تعرف اللغة الإسبانية؟

وبذلك، نكون قد أكملنا مساعد الصوت من البداية إلى النهاية، باستخدام أدوات الصوت 🤗 التي تعلمتها طوال
هذه الدورة، مع رشّة من سحر LLM في النهاية. هناك العديد من الإضافات التي يمكننا إجراؤها لتحسين
مساعد الصوت. أولاً، يصنف نموذج تصنيف الصوت 35 تسمية مختلفة. يمكننا استخدام نموذج تصنيف ثنائي أصغر وأكثر خفة، والذي يتوقع فقط ما إذا كانت كلمة الاستيقاظ قد تم التحدث بها أم لا. ثانيًا، نقوم بتحميل مسبق
جميع النماذج مسبقًا وإبقائها قيد التشغيل على جهازنا. إذا أردنا توفير الطاقة، فسنقوم بتحميل كل نموذج فقط في
الوقت الذي كان مطلوبًا، ثم نقوم بإلغاء تحميلها بعد ذلك. ثالثًا، نفتقد نموذج اكتشاف نشاط الصوت في دالة النسخ الخاصة بنا، حيث نقوم بالنسخ لمدة ثابتة، والتي تكون في بعض الحالات طويلة جدًا، وفي حالات أخرى قصيرة جدًا.

## تعميم أي شيء 🪄

حتى الآن، رأينا كيف يمكننا إنشاء مخرجات صوتية باستخدام مساعد الصوت لدينا Marvin. للانتهاء، سنقوم بالتوضيح
كيف يمكننا تعميم هذه المخرجات الصوتية إلى نص وصوت وصورة.

سنستخدم [وكلاء المحولات](https://huggingface.co/docs/transformers/transformers_agents) لبناء مساعدنا.
يوفر وكلاء المحولات واجهة برمجة تطبيقات اللغة الطبيعية فوق مكتبات 🤗 Transformers وDiffusers، حيث يفسر
إدخال اللغة الطبيعية باستخدام LLM مع مطالبات مصممة بعناية، واستخدام مجموعة من الأدوات المختارة لتوفير
مخرجات متعددة الوسائط.

دعونا ننتقل إلى إنشاء وكيل. هناك [ثلاثة LLMs متاحة](https://huggingface.co/docs/transformers/transformers_agents#quickstart)
للوكلاء المحولين، اثنان منها مفتوح المصدر ومتاحان مجانًا على Hugging Face Hub. والثالث هو نموذج من OpenAI
يتطلب مفتاح واجهة برمجة تطبيقات OpenAI. سنستخدم نموذج [Bigcode Starcoder](https://huggingface.co/bigcode/starcoder) المجاني
في هذا المثال، ولكن يمكنك أيضًا تجربة أي من LLMs الأخرى المتاحة:

```python
from transformers import HfAgent

agent = HfAgent(
url_endpoint="https://api-inference.huggingface.co/models/bigcode/starcoder"
)
```

لاستخدام الوكيل، ما علينا سوى استدعاء `agent.run` مع مطالبة النص الخاصة بنا. كمثال، سنطلب منه ذلك
لإنشاء صورة لقطة 🐈 (يبدو أفضل قليلاً من هذا الرمز التعبيري):

```python
agent.run("Generate an image of a cat")
```

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/audio-course-images/resolve/main/generated_cat.png">
</div>

<Tip>
لاحظ أن المكالمة الأولى ستؤدي إلى تنزيل أوزان النموذج، والتي قد تستغرق بعض الوقت اعتمادًا على سرعة تنزيل Hub الخاصة بك.
</Tip>

من السهل القيام بذلك! فسر الوكيل مطالبتنا، واستخدم [Stable Diffusion](https://huggingface.co/docs/diffusers/using-diffusers/conditional_image_generation)
تحت الغطاء لتوليد الصورة، دون أن نضطر إلى القلق بشأن تحميل النموذج أو كتابة الدالة أو تنفيذ التعليمات البرمجية.

الآن يمكننا استبدال دالة استعلام LLM وخطوة تركيب النص الخاصة بنا بالوكيل الخاص بنا في مساعد الصوت لدينا،
نظرًا لأن الوكيل سيتولى رعاية كلتا الخطوتين نيابة عنا:

```python
launch_fn()
transcription = transcribe()
agent.run(transcription)
```

جرب التحدث بنفس المطالبة "Generate an image of a cat" وشاهد كيف يتعامل النظام معها. إذا طلبت من الوكيل استعلامًا بسيطًا / إجابة، فسيستجيب الوكيل بإجابة نصية. يمكنك تشجيعه على إنشاء مخرجات متعددة الوسائط
عن طريق طلب صورة أو صوت منه. على سبيل المثال، يمكنك أن تطلب منه: "Generate an image of a cat، قم بتعليقها، وتحدث عنها".

بينما يكون الوكيل أكثر مرونة من مساعدنا الأول Marvin 🤖، فإن تعميم مهمة مساعد الصوت بهذه الطريقة
قد يؤدي إلى ضعف الأداء في استعلامات مساعد الصوت القياسية. للتعافي من الأداء، يمكنك تجربة استخدام
نقطة تفتيش LLM أكثر قوة، مثل تلك التي من OpenAI، أو تحديد مجموعة من [الأدوات المخصصة](https://huggingface.co/docs/transformers/transformers_agents#custom-tools)
محددة لمهمة مساعد الصوت.