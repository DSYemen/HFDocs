# معالجة مجموعة بيانات صوتية قبل التدريب

تحميل مجموعة بيانات باستخدام 🤗 Datasets هو نصف المتعة فقط. إذا كنت تخطط لاستخدامها إما لتدريب نموذج أو للاستنتاج، فستحتاج إلى معالجة البيانات مسبقًا. بشكل عام، ستشمل هذه الخطوة ما يلي:

* إعادة أخذ العينات من البيانات الصوتية
* تصفية مجموعة البيانات
* تحويل البيانات الصوتية إلى المدخلات المتوقعة للنموذج

## إعادة أخذ العينات من البيانات الصوتية

تقوم دالة `load_dataset` بتنزيل أمثلة صوتية بمعدل أخذ العينات الذي تم نشره بها. وهذا ليس دائمًا معدل أخذ العينات الذي يتوقعه النموذج الذي تخطط لتدريبه أو استخدامه للاستنتاج. إذا كان هناك اختلاف بين معدلات أخذ العينات، فيمكنك إعادة أخذ عينات الصوت بمعدل أخذ العينات المتوقع للنموذج.

تمت معايرة معظم النماذج المتاحة مسبقًا على مجموعات بيانات صوتية بمعدل أخذ عينات يبلغ 16 كيلو هرتز.

عندما استكشفنا مجموعة بيانات MINDS-14، ربما لاحظت أنها معاينة عند 8 كيلو هرتز، مما يعني أننا سنحتاج على الأرجح إلى زيادة دقة العينات.

للقيام بذلك، استخدم طريقة 🤗 Datasets' `cast_column`. لا تغير هذه العملية الصوت في المكان، ولكنها تشير إلى إعادة أخذ عينات من الأمثلة الصوتية أثناء التنقل عند تحميلها. سيؤدي الكود التالي إلى تعيين معدل أخذ العينات إلى 16 كيلو هرتز:

```py
from datasets import Audio

minds = minds.cast_column("audio", Audio(sampling_rate=16_000))
```

أعد تحميل أول مثال صوتي في مجموعة بيانات MINDS-14، وتحقق من أنه تمت إعادة أخذ العينات منه إلى معدل أخذ العينات المطلوب:

```py
minds[0]
```

**الناتج:**

```out
{
"path": "/root/.cache/huggingface/datasets/downloads/extracted/f14948e0e84be638dd7943ac36518a4cf3324e8b7aa331c5ab11541518e9368c/en-AU~PAY_BILL/response_4.wav",
"audio": {
"path": "/root/.cache/huggingface/datasets/downloads/extracted/f14948e0e84be638dd7943ac36518a4cf3324e8b7aa331c5ab11541518e9368c/en-AU~PAY_BILL/response_4.wav",
"array": array(
[
2.0634243e-05,
1.9437837e-04,
2.2419340e-04,
...,
9.3852862e-04,
1.1302452e-03,
7.1531429e-04,
],
dtype=float32,
),
"sampling_rate": 16000,
},
"transcription": "I would like to pay my electricity bill using my card can you please assist",
"intent_class": 13,
}
```

قد تلاحظ أن قيم المصفوفة مختلفة الآن أيضًا. هذا لأننا حصلنا الآن على ضعف عدد قيم السعة لكل واحدة كانت لدينا من قبل.

<Tip>

💡 بعض المعلومات الأساسية حول إعادة أخذ العينات: إذا تم أخذ عينات من إشارة صوتية بمعدل 8 كيلو هرتز، بحيث يكون لديها 8000 قراءة عينة في الثانية، فنحن نعلم أن الصوت لا يحتوي على أي ترددات أعلى من 4 كيلو هرتز. هذا مضمون من قبل نظرية أخذ العينات ني كوست. وبسبب هذا، يمكننا أن نكون متأكدين من أنه بين نقاط أخذ العينات، فإن الإشارة الأصلية المستمرة تجعل المنحنى سلسًا دائمًا. إن إعادة أخذ العينات بمعدل أعلى هي مسألة حساب قيم العينات الإضافية التي تتناسب مع القيم الموجودة، عن طريق تقريب هذا المنحنى. من ناحية أخرى، يتطلب تقليل الدقة أولاً تصفية أي ترددات أعلى من حد ني كوست الجديد، قبل تقدير نقاط العينة الجديدة. وبعبارة أخرى، لا يمكنك تقليل الدقة بمقدار 2x ببساطة عن طريق التخلص من كل عينة أخرى - سيؤدي هذا إلى تشويهات في الإشارة تسمى الأسماء المستعارة. إن إعادة أخذ العينات بشكل صحيح أمر صعب ويترك لأفضل المكتبات التي تم اختبارها مثل librosa أو 🤗 Datasets.

</Tip>

## تصفية مجموعة البيانات

قد تحتاج إلى تصفية البيانات بناءً على بعض المعايير. إحدى الحالات الشائعة تنطوي على الحد من الأمثلة الصوتية لمدة معينة. على سبيل المثال، قد نريد تصفية أي أمثلة أطول من 20 ثانية لمنع أخطاء نفاد الذاكرة عند تدريب نموذج.

يمكننا القيام بذلك باستخدام طريقة 🤗 Datasets' `filter` وتمرير دالة بها منطق التصفية إليها. دعنا نبدأ بكتابة دالة تشير إلى الأمثلة التي يجب الاحتفاظ بها والأمثلة التي يجب التخلص منها. تعيد هذه الدالة، `is_audio_length_in_range`، `True` إذا كانت العينة أقصر من 20 ثانية، و`False` إذا كانت أطول من 20 ثانية.

```py
MAX_DURATION_IN_SECONDS = 20.0


def is_audio_length_in_range(input_length):
return input_length < MAX_DURATION_IN_SECONDS
```

يمكن تطبيق دالة التصفية على عمود في مجموعة البيانات، ولكن ليس لدينا عمود بمدة مسار الصوت في هذه المجموعة. ومع ذلك، يمكننا إنشاء واحد، والتصفية بناءً على القيم في هذا العمود، ثم إزالته.

```py
# use librosa to get example's duration from the audio file
new_column = [librosa.get_duration(path=x) for x in minds["path"]]
minds = minds.add_column("duration", new_column)

# use 🤗 Datasets' `filter` method to apply the filtering function
minds = minds.filter(is_audio_length_in_range, input_columns=["duration"])

# remove the temporary helper column
minds = minds.remove_columns(["duration"])
minds
```

**الناتج:**

```out
Dataset({features: ["path", "audio", "transcription", "intent_class"], num_rows: 624})
```

يمكننا التحقق من أن مجموعة البيانات تمت تصفيتها من 654 مثال إلى 624.

## معالجة البيانات الصوتية

أحد الجوانب الأكثر تحديًا للعمل مع مجموعات البيانات الصوتية هو إعداد البيانات بالشكل الصحيح لتدريب النموذج. كما رأيت، تأتي البيانات الصوتية الخام كمصفوفة من قيم العينات. ومع ذلك، تتوقع النماذج التي تمت معايرتها مسبقًا، سواء كنت تستخدمها للاستنتاج أو تريد ضبطها الدقيق لمهامك، تحويل البيانات الخام إلى ميزات المدخلات. قد تختلف متطلبات ميزات الإدخال من نموذج إلى آخر - فهي تعتمد على بنية النموذج والبيانات التي تم تدريبه مسبقًا عليها. النبأ السار هو أنه بالنسبة لكل نموذج صوتي مدعوم، توفر 🤗 Transformers فئة مستخرج الميزات التي يمكنها تحويل البيانات الصوتية الخام إلى ميزات الإدخال التي يتوقعها النموذج.

لذا، ماذا تفعل فئة مستخرج الميزات بالبيانات الصوتية الخام؟ دعنا نلقي نظرة على مستخرج ميزات [Whisper](https://huggingface.co/papers/2212.04356) لفهم بعض التحولات الشائعة لاستخراج الميزات. Whisper هو نموذج معاير مسبقًا للتعرف التلقائي على الكلام (ASR) نشره Alec Radford et al. من OpenAI في سبتمبر 2022.

أولاً، يقوم مستخرج ميزات Whisper بضبط/تقليم دفعة من الأمثلة الصوتية بحيث يكون لجميع الأمثلة طول إدخال يبلغ 30 ثانية. يتم ضبط الأمثلة الأقصر من هذا عن طريق الضبط إلى 30 ثانية عن طريق إلحاق أصفار في نهاية التسلسل (تتوافق الأصفار في إشارة صوتية مع عدم وجود إشارة أو صمت). يتم تقليم الأمثلة الأطول من 30 ثانية إلى 30 ثانية.

نظرًا لأن جميع العناصر في الدفعة يتم ضبطها/تقليمها إلى طول أقصى في مساحة الإدخال، فلا توجد حاجة إلى قناع اهتمام. Whisper فريد من نوعه في هذا الصدد، تتطلب معظم النماذج الصوتية الأخرى قناع اهتمام يوضح المكان الذي تم فيه ضبط التسلسلات، وبالتالي المكان الذي يجب تجاهله في آلية الاهتمام الذاتي. تم تدريب Whisper للعمل بدون قناع اهتمام والاستنتاج مباشرة من إشارات الكلام حيث يجب تجاهل المدخلات.

والعملية الثانية التي يقوم بها مستخرج ميزات Whisper هي تحويل المصفوفات الصوتية المضبوطة إلى مخططات Mel-Spectrogram. كما تتذكر، تصف هذه المخططات الطيفية كيف تتغير ترددات إشارة بمرور الوقت، معبر عنها على مقياس ميل ومقاسة بالديسيبل (الجزء اللوغاريتمي) لجعل الترددات والسعات أكثر تمثيلا للسمع البشري.

يمكن تطبيق جميع هذه التحولات على بياناتك الصوتية الخام ببضع سطور من التعليمات البرمجية. دعنا ننتقل إلى تحميل مستخرج الميزات من نقطة التحقق Whisper المعايرة مسبقًا لتكون جاهزة لبياناتنا الصوتية:

```py
from transformers import WhisperFeatureExtractor

feature_extractor = WhisperFeatureExtractor.from_pretrained("openai/whisper-small")
```

بعد ذلك، يمكنك كتابة دالة لمعالجة مثال صوتي واحد عن طريق تمريره عبر `feature_extractor`.

```py
def prepare_dataset(example):
audio = example["audio"]
features = feature_extractor(
audio["array"], sampling_rate=audio["sampling_rate"], padding=True
)
return features
```

يمكننا تطبيق دالة إعداد البيانات على جميع أمثلة التدريب الخاصة بنا باستخدام طريقة الخرائط 🤗 Datasets:

```py
minds = minds.map(prepare_dataset)
minds
```

**الناتج:**

```out
Dataset(
{
features: ["path", "audio", "transcription", "intent_class", "input_features"],
num_rows: 624,
}
)
```

بسهولة، لدينا الآن مخططات Mel-Spectrogram كميزات `input_features` في مجموعة البيانات.

دعنا نرسم مثالاً واحدًا في مجموعة بيانات `minds`:

```py
import numpy as np

example = minds[0]
input_features = example["input_features"]

plt.figure().set_figwidth(12)
librosa.display.specshow(
np.asarray(input_features[0]),
x_axis="time",
y_axis="mel",
sr=feature_extractor.sampling_rate,
hop_length=feature_extractor.hop_length,
)
plt.colorbar()
```

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/audio-course-images/resolve/main/log_mel_whisper.png" alt="Log mel spectrogram plot">
</div>

الآن يمكنك أن ترى كيف يبدو إدخال الصوت لنموذج Whisper بعد المعالجة المسبقة.

تعتني فئة مستخرج ميزات النموذج بتحويل البيانات الصوتية الخام إلى التنسيق الذي يتوقعه النموذج. ومع ذلك، فإن العديد من المهام التي تنطوي على الصوت متعددة الوسائط، على سبيل المثال التعرف على الكلام. في مثل هذه الحالات، تقدم 🤗 Transformers أيضًا رموزًا محددة للنموذج لمعالجة إدخالات النص. للحصول على نظرة متعمقة حول رموز الاستدعاء، يرجى الرجوع إلى [دورة NLP](https://huggingface.co/course/chapter2/4).

يمكنك تحميل مستخرج الميزات والرموز النمطية لـ Whisper وغيرها من النماذج متعددة الوسائط بشكل منفصل، أو يمكنك تحميل كلاهما عبر ما يسمى المعالج. لجعل الأمور أكثر بساطة، استخدم `AutoProcessor` لتحميل مستخرج ميزات المعالج ومعالجته من نقطة التحقق، مثل هذا:

```py
from transformers import AutoProcessor

processor = AutoProcessor.from_pretrained("openai/whisper-small")
```

لقد أوضحنا هنا خطوات الإعداد الأساسية للبيانات. بالطبع، قد تتطلب البيانات المخصصة معالجة مسبقة أكثر تعقيدًا. في هذه الحالة، يمكنك توسيع دالة `prepare_dataset` لأداء أي نوع من تحويلات البيانات المخصصة. باستخدام 🤗 Datasets، إذا كنت تستطيع كتابتها كدالة Python، فيمكنك [تطبيقها](https://huggingface.co/docs/datasets/audio_process) على مجموعة البيانات الخاصة بك!