# التعرف التلقائي على الكلام باستخدام خط أنابيب

Automatic Speech Recognition (ASR) هي مهمة تتضمن نسخ تسجيل صوتي للنطق إلى نص. لهذه المهمة تطبيقات عملية عديدة، بدءًا من إنشاء تعليقات توضيحية مغلقة للفيديوهات إلى تمكين الأوامر الصوتية للمساعدين الافتراضيين مثل Siri وAlexa.

في هذا القسم، سنستخدم خط أنابيب "التعرف التلقائي على الكلام" لنسخ تسجيل صوتي لشخص يسأل عن سداد فاتورة باستخدام مجموعة بيانات MINDS-14 نفسها كما هو موضح سابقًا.

لتبدأ، قم بتحميل مجموعة البيانات وقم بزيادة عيناتها إلى 16 كيلو هرتز كما هو موضح في [تصنيف الصوت باستخدام خط الأنابيب](audio_classification_pipeline)، إذا لم تقم بذلك بعد.

لنسخ تسجيل صوتي، يمكننا استخدام خط أنابيب "النسخ التلقائي للنص" من 🤗 Transformers. دعنا ننشئ مثالاً على خط الأنابيب:

```py
from transformers import pipeline

asr = pipeline("automatic-speech-recognition")
```

بعد ذلك، سنأخذ مثالاً من مجموعة البيانات ونمرر بياناته الخام إلى خط الأنابيب:

```py
example = minds[0]
asr(example["audio"]["array"])
```

**الإخراج:**

```out
{"text": "I WOULD LIKE TO PAY MY ELECTRICITY BILL USING MY COD CAN YOU PLEASE ASSIST"}
```

دعونا نقارن هذا الإخراج بما هو عليه بالفعل نسخ هذا المثال:

```py
example["english_transcription"]
```

**الإخراج:**

```out
"I would like to pay my electricity bill using my card, can you please assist?"
```

يبدو أن النموذج قام بعمل جيد جدًا في نسخ الصوت! لقد أخطأ في كلمة واحدة فقط ("card") مقارنة بالنسخ الأصلي، وهو أمر جيد بالنظر إلى أن المتحدث لديه لهجة أسترالية، حيث يكون حرف "r" صامتًا غالبًا. ومع ذلك، لا أوصي بمحاولة سداد فاتورة الكهرباء التالية باستخدام سمكة!

بشكل افتراضي، يستخدم هذا الخط نموذجًا مدربًا للتعرف التلقائي على الكلام باللغة الإنجليزية، وهو ما يناسب هذا المثال. إذا كنت تريد تجربة نسخ أقسام أخرى من مجموعة بيانات MINDS-14 بلغات مختلفة، فيمكنك العثور على نموذج تم تدريبه مسبقًا [على 🤗 Hub](https://huggingface.co/models؟pipeline_tag=automatic-speech-recognition&language=fr&sort=downloads). يمكنك تصفية قائمة النماذج حسب المهمة أولاً، ثم حسب اللغة. بمجرد العثور على النموذج الذي تريده، قم بتمرير اسمه كوسيط "model" إلى خط الأنابيب.

دعونا نجرب هذا الأمر بالنسبة للجزء الألماني من مجموعة بيانات MINDS-14. قم بتحميل مجموعة فرعية "de-DE":

```py
from datasets import load_dataset
from datasets import Audio

minds = load_dataset("PolyAI/minds14", name="de-DE", split="train")
minds = minds.cast_column("audio", Audio(sampling_rate=16_000))
```

احصل على مثال وشاهد ما يجب أن يكون النسخ:

```py
example = minds[0]
example["transcription"]
```

**الإخراج:**

```out
"ich möchte gerne Geld auf mein Konto einzahlen"
```

ابحث عن نموذج التعرف التلقائي على الكلام باللغة الألمانية على 🤗 Hub، وقم بتنفيذ خط أنابيب، وانسخ المثال:

```py
from transformers import pipeline

asr = pipeline("automatic-speech-recognition", model="maxidl/wav2vec2-large-xlsr-german")
asr(example["audio"]["array"])
```

**الإخراج:**

```out
{"text": "ich möchte gerne geld auf mein konto einzallen"}
```

هذا صحيح أيضًا!

عند العمل على حل مهمتك الخاصة، يعد البدء بخط أنابيب بسيط مثل تلك التي أظهرناها في هذه الوحدة أداة قيمة تقدم العديد من الفوائد:

- قد يوجد نموذج مدرب مسبقًا يحل مهمتك بالفعل بشكل جيد، مما يوفر لك الكثير من الوقت.
- تقوم وظيفة pipeline() بمعالجة جميع عمليات المعالجة المسبقة واللاحقة لك، لذا لا داعي للقلق بشأن الحصول على البيانات بتنسيق مناسب للنموذج.
- إذا لم تكن النتيجة مثالية، فلا يزال بإمكانك الحصول على خط أساس سريع للضبط الدقيق في المستقبل.
- بمجرد ضبط نموذج على بياناتك المخصصة ومشاركته على Hub، سيتمكن المجتمع بأكمله من استخدامه بسرعة وسهولة عبر طريقة pipeline()، مما يجعل الذكاء الاصطناعي أكثر سهولة.