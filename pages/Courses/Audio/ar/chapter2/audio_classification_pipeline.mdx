# تصنيف الصوت باستخدام خط أنابيب

يشتمل تصنيف الصوت على تعيين تسمية واحدة أو أكثر لتسجيل صوتي بناءً على محتواه. يمكن أن تتوافق التسميات مع فئات صوت مختلفة، مثل الموسيقى أو الكلام أو الضوضاء، أو فئات أكثر تحديدًا مثل أغاني الطيور أو أصوات محرك السيارة.

قبل الغوص في تفاصيل كيفية عمل أكثر محولات الصوت شيوعًا، وقبل ضبط نموذج مخصص، دعونا نرى كيف يمكنك استخدام نموذج مُدرب مسبقًا جاهز لتصنيف الصوت باستخدام بضع سطور فقط من التعليمات البرمجية مع 🤗 Transformers.

لننتقل الآن إلى استخدام نفس مجموعة بيانات [MINDS-14](https://huggingface.co/datasets/PolyAI/minds14) التي استكشفتها في الوحدة السابقة. إذا كنت تتذكر، تحتوي MINDS-14 على تسجيلات لأشخاص يطرحون أسئلة على نظام مصرفي إلكتروني بعدة لغات ولهجات، ولديها `intent_class` لكل تسجيل. يمكننا تصنيف التسجيلات حسب نية المكالمة.

وكما هو الحال دائمًا، دعونا نبدأ بتحميل مجموعة فرعية `en-AU` من البيانات لتجربة خط الأنابيب، ونرفعها إلى معدل أخذ العينات 16 كيلو هرتز وهو ما تتطلبه معظم نماذج الكلام.

```py
from datasets import load_dataset
from datasets import Audio

minds = load_dataset("PolyAI/minds14", name="en-AU", split="train")
minds = minds.cast_column("audio", Audio(sampling_rate=16_000))
```

لتصنيف تسجيل صوتي إلى مجموعة من الفئات، يمكننا استخدام خط أنابيب `audio-classification` من 🤗 Transformers. في حالتنا، نحتاج إلى نموذج تمت معايرته لتصنيف النوايا، وتحديدًا على مجموعة بيانات MINDS-14. لحسن الحظ، يحتوي Hub على نموذج يقوم بذلك بالضبط! دعونا نقوم بتحميله باستخدام وظيفة `pipeline()`:

```py
from transformers import pipeline

classifier = pipeline(
"audio-classification",
model="anton-l/xtreme_s_xlsr_300m_minds14",
)
```

يتوقع هذا الخط أن تكون بيانات الصوت على شكل مصفوفة NumPy. سيتم التعامل مع جميع المعالجة المسبقة لبيانات الصوت الخام بشكل ملائم بواسطة خط الأنابيب. دعونا نختار مثالًا لتجربته:

```py
example = minds[0]
```

إذا كنت تتذكر بنية مجموعة البيانات، يتم تخزين بيانات الصوت الخام في مصفوفة NumPy تحت `["audio"]["array"]`. دعنا نمررها مباشرة إلى `classifier`:

```py
classifier(example["audio"]["array"])
```

**الإخراج:**

```out
[
{"score": 0.9631525278091431, "label": "pay_bill"},
{"score": 0.02819698303937912, "label": "freeze"},
{"score": 0.0032787492964416742, "label": "card_issues"},
{"score": 0.0019414445850998163, "label": "abroad"},
{"score": 0.0008378693601116538, "label": "high_value_payment"},
]
```

النموذج واثق جدًا من أن المتصل كان ينوي الاستفسار عن دفع فاتورته. دعونا نرى ما هو التصنيف الفعلي لهذا المثال:

```py
id2label = minds.features["intent_class"].int2str
id2label(example["intent_class"])
```

**الإخراج:**

```out
"pay_bill"
```

رائع! كان التصنيف المتوقع صحيحًا! هنا كنا محظوظين بالعثور على نموذج يمكنه تصنيف التسميات التي نحتاجها بالضبط. في كثير من الأحيان، عند التعامل مع مهمة تصنيف، لا تكون مجموعة فئات النموذج المُدرب مسبقًا هي نفسها بالضبط كما الفئات التي تحتاجها النموذج للتمييز بينها. في هذه الحالة، يمكنك ضبط نموذج مُدرب مسبقًا "معايرته" لمجموعة التسميات الخاصة بك بالضبط. سنتعلم كيفية القيام بذلك في الوحدات القادمة. الآن، دعونا نلقي نظرة على مهمة شائعة جدًا أخرى في معالجة الكلام، وهي التعرف التلقائي على الكلام.