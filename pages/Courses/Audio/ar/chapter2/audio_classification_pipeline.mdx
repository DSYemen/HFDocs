# تصنيف الصوت باستخدام خط أنابيب

يتضمن تصنيف الصوت تعيين تسمية واحدة أو أكثر لتسجيل صوتي بناءً على محتواه. يمكن أن تتوافق التسميات
مع فئات صوتية مختلفة، مثل الموسيقى أو الكلام أو الضوضاء، أو فئات أكثر تحديدًا مثل
أغاني الطيور أو أصوات محرك السيارة.

قبل الغوص في تفاصيل كيفية عمل محولات الصوت الأكثر شعبية، وقبل ضبط نموذج مخصص، دعنا
نرى كيف يمكنك استخدام نموذج جاهز مسبقًا لتصنيف الصوت ببضع أسطر من التعليمات البرمجية مع 🤗 Transformers.

لنستخدم نفس مجموعة بيانات [MINDS-14](https://huggingface.co/datasets/PolyAI/minds14) التي قمت باستكشافها
في الوحدة السابقة. إذا كنت تتذكر، تحتوي MINDS-14 على تسجيلات لأشخاص يطرحون أسئلة على نظام مصرفي إلكتروني بعدة
لغات ولهجات، ولديها `intent_class` لكل تسجيل. يمكننا تصنيف التسجيلات حسب نية المكالمة.

كما فعلنا من قبل، دعنا نبدأ بتحميل مجموعة فرعية `en-AU` من البيانات لتجربة خط الأنابيب، ورفع معدل العينة إلى 16 كيلو هرتز
معدل العينة وهو ما تتطلبه معظم نماذج الكلام.

```py
from datasets import load_dataset
from datasets import Audio

minds = load_dataset("PolyAI/minds14", name="en-AU", split="train")
minds = minds.cast_column("audio", Audio(sampling_rate=16_000))
```

لتصنيف تسجيل صوتي إلى مجموعة من الفئات، يمكننا استخدام خط أنابيب `audio-classification` من 🤗 Transformers.
في حالتنا، نحتاج إلى نموذج تم ضبطه مسبقًا لتصنيف النوايا، وتحديدًا على
مجموعة بيانات MINDS-14. لحسن الحظ، يوفر لنا المركز نموذجًا يقوم بذلك بالضبط! دعنا نحمله باستخدام دالة `pipeline()`:

```py
from transformers import pipeline

classifier = pipeline(
    "audio-classification",
    model="anton-l/xtreme_s_xlsr_300m_minds14",
)
```

يتوقع هذا الخط أن تكون بيانات الصوت على شكل مصفوفة NumPy. سيتم التعامل مع جميع معالجة بيانات الصوت الخام بشكل مناسب
لنا بواسطة خط الأنابيب. دعنا نختار مثالًا لتجربته:

```py
example = minds[0]
```

إذا كنت تتذكر بنية مجموعة البيانات، يتم تخزين بيانات الصوت الخام في مصفوفة NumPy تحت `["audio"]["array"]`. دعنا
نمررها مباشرة إلى `classifier`:

```py
classifier(example["audio"]["array"])
```

**الناتج:**
```out
[
    {"score": 0.9631525278091431, "label": "pay_bill"},
    {"score": 0.02819698303937912, "label": "freeze"},
    {"score": 0.0032787492964416742, "label": "card_issues"},
    {"score": 0.0019414445850998163, "label": "abroad"},
    {"score": 0.0008378693601116538, "label": "high_value_payment"},
]
```

النموذج واثق جدًا من أن المتصل كان ينوي معرفة كيفية دفع فاتورته. دعنا نرى ما هي التسمية الفعلية لهذا
المثال:

```py
id2label = minds.features["intent_class"].int2str
id2label(example["intent_class"])
```

**الناتج:**
```out
"pay_bill"
```

هيا! كانت التسمية المتوقعة صحيحة! هنا كنا محظوظين بالعثور على نموذج يمكنه تصنيف التسميات التي نحتاجها بالضبط.
في كثير من الأحيان، عند التعامل مع مهمة التصنيف، لا تكون مجموعة فئات النموذج الجاهز مسبقًا مطابقة تمامًا
للفئات التي تحتاج النموذج أن يميزها. في هذه الحالة، يمكنك ضبط نموذج جاهز مسبقًا لجعله "متوافقًا" مع
مجموعة فئاتك بالضبط. سنتعلم كيفية القيام بذلك في الوحدات القادمة. الآن، دعنا نلقي نظرة على مهمة أخرى شائعة جدًا في معالجة الكلام، _التعرف التلقائي على الكلام_.