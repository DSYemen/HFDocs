# المعالجة المسبقة لمجموعة البيانات الصوتية 

تحميل مجموعة بيانات باستخدام 🤗 Datasets هو نصف المتعة فقط. إذا كنت تخطط لاستخدامها إما لتدريب نموذج أو لتشغيل الاستدلال، فستحتاج إلى معالجة البيانات مسبقًا. بشكل عام، سيتضمن ذلك الخطوات التالية:

* إعادة أخذ عينات من البيانات الصوتية
* تصفية مجموعة البيانات
* تحويل البيانات الصوتية إلى المدخلات المتوقعة للنموذج

## إعادة أخذ عينات من البيانات الصوتية

يقوم دالة `load_dataset` بتنزيل الأمثلة الصوتية بمعدل أخذ العينات الذي تم نشره به. هذا ليس
دائماً معدل أخذ العينات المتوقع من قبل نموذج تخطط لتدريبه، أو استخدامه للاستدلال. إذا كان هناك اختلاف بين
معدلات أخذ العينات، يمكنك إعادة أخذ عينات الصوت إلى معدل أخذ العينات المتوقع من النموذج.

معظم النماذج المسبقة التدريب المتوفرة تم تدريبها مسبقًا على مجموعات بيانات صوتية بمعدل أخذ عينات 16 كيلو هرتز.
عندما استكشفنا مجموعة بيانات MINDS-14، ربما لاحظت أنها تم أخذ عينات منها بمعدل 8 كيلو هرتز، مما يعني أننا سنحتاج على الأرجح
إلى زيادة أخذ العينات.

للقيام بذلك، استخدم طريقة `cast_column` في 🤗 Datasets. هذه العملية لا تغير الصوت في مكانه، ولكنها تشير
إلى مجموعات البيانات لإعادة أخذ عينات الأمثلة الصوتية أثناء التنقل عندما يتم تحميلها. سيقوم الكود التالي بضبط معدل أخذ العينات
إلى 16 كيلو هرتز:

```py
from datasets import Audio

minds = minds.cast_column("audio", Audio(sampling_rate=16_000))
```

أعد تحميل المثال الصوتي الأول في مجموعة بيانات MINDS-14، وتحقق من أنه تم إعادة أخذ عينات منه إلى معدل أخذ العينات المطلوب:

```py
minds[0]
```

**الإخراج:**
```out
{
    "path": "/root/.cache/huggingface/datasets/downloads/extracted/f14948e0e84be638dd7943ac36518a4cf3324e8b7aa331c5ab11541518e9368c/en-AU~PAY_BILL/response_4.wav",
    "audio": {
        "path": "/root/.cache/huggingface/datasets/downloads/extracted/f14948e0e84be638dd7943ac36518a4cf3324e8b7aa331c5ab11541518e9368c/en-AU~PAY_BILL/response_4.wav",
        "array": array(
            [
                2.0634243e-05,
                1.9437837e-04,
                2.2419340e-04,
                ...,
                9.3852862e-04,
                1.1302452e-03,
                7.1531429e-04,
            ],
            dtype=float32,
        ),
        "sampling_rate": 16000,
    },
    "transcription": "I would like to pay my electricity bill using my card can you please assist",
    "intent_class": 13,
}
```

قد تلاحظ أن قيم المصفوفة مختلفة أيضًا. هذا لأننا حصلنا الآن على ضعف عدد قيم السعة لكل
واحد كان لدينا من قبل.

<نصيحة>
💡 بعض المعلومات الأساسية حول إعادة أخذ العينات: إذا تم أخذ عينات من إشارة صوتية بمعدل 8 كيلو هرتز، بحيث يكون لديها 8000 قراءة عينة في
الثانية، نعلم أن الصوت لا يحتوي على أي ترددات تزيد عن 4 كيلو هرتز. هذا مضمون من قبل نظرية أخذ العينات
نيكوست. بسبب هذا، يمكننا أن نكون متأكدين من أنه بين نقاط أخذ العينات، فإن الإشارة المستمرة الأصلية دائمًا
تصنع منحنى ناعم. زيادة أخذ العينات إلى معدل أخذ عينات أعلى هي مسألة حساب قيم عينة إضافية تذهب بين
الموجودة، عن طريق تقريب هذه المنحنى. ومع ذلك، يتطلب خفض معدل أخذ العينات أن نقوم أولاً بتصفية أي ترددات
التي ستكون أعلى من حد نيكوست الجديد، قبل تقدير نقاط العينة الجديدة. وبعبارة أخرى، لا يمكنك
خفض معدل أخذ العينات بعامل 2x ببساطة عن طريق التخلص من كل عينة أخرى - سيؤدي هذا إلى تشوهات في الإشارة تسمى
أسماء مستعارة. القيام بإعادة أخذ العينات بشكل صحيح أمر صعب ومن الأفضل تركه للمكتبات التي تم اختبارها جيدًا مثل librosa أو 🤗 Datasets.
</نصيحة>

## تصفية مجموعة البيانات

قد تحتاج إلى تصفية البيانات بناءً على بعض المعايير. إحدى الحالات الشائعة تتضمن تحديد الأمثلة الصوتية لفترة زمنية معينة. على سبيل المثال، قد نرغب في تصفية أي أمثلة أطول من 20 ثانية لمنع أخطاء نفاد الذاكرة
عند تدريب نموذج.

يمكننا القيام بذلك باستخدام طريقة `filter` في 🤗 Datasets وتمرير دالة بها منطق التصفية إليها. دعنا نبدأ بكتابة
دالة تشير إلى الأمثلة التي يجب الاحتفاظ بها والأمثلة التي يجب التخلص منها. هذه الدالة، `is_audio_length_in_range`،
تعيد `True` إذا كانت العينة أقصر من 20 ثانية، و `False` إذا كانت أطول من 20 ثانية.

```py
MAX_DURATION_IN_SECONDS = 20.0


def is_audio_length_in_range(input_length):
    return input_length < MAX_DURATION_IN_SECONDS
```

يمكن تطبيق دالة التصفية على عمود في مجموعة البيانات ولكن ليس لدينا عمود بمدة المسار الصوتي في هذا
مجموعة البيانات. ومع ذلك، يمكننا إنشاء واحد، والتصفية بناءً على القيم في هذا العمود، ثم إزالته.

```py
# استخدم librosa للحصول على مدة المثال من ملف الصوت
new_column = [librosa.get_duration(path=x) for x in minds["path"]]
minds = minds.add_column("duration", new_column)

# استخدم طريقة `filter` في 🤗 Datasets لتطبيق دالة التصفية
minds = minds.filter(is_audio_length_in_range, input_columns=["duration"])

# إزالة العمود المساعد المؤقت
minds = minds.remove_columns(["duration"])
minds
```

**الإخراج:**
```out
Dataset({features: ["path", "audio", "transcription", "intent_class"], num_rows: 624})
```

يمكننا التحقق من أنه تم تصفية مجموعة البيانات من 654 مثالًا إلى 624.

## معالجة البيانات الصوتية مسبقًا

أحد أصعب جوانب العمل مع مجموعات البيانات الصوتية هو إعداد البيانات بالشكل الصحيح لتدريب النموذج
. كما رأيت، تأتي البيانات الصوتية الخام كمصفوفة من قيم العينة. ومع ذلك، تتوقع النماذج المسبقة التدريب، سواء كنت تستخدمها
للاستدلال، أو تريد ضبطها الدقيق لمهمتك، تحويل البيانات الخام إلى ميزات الإدخال. قد تختلف متطلبات ميزات الإدخال من نموذج إلى آخر - فهي تعتمد على بنية النموذج، والبيانات التي تم تدريبها مسبقًا. والخبر السار هو أنه لكل نموذج صوتي مدعوم، 🤗 Transformers يقدم فئة مستخرج الميزات
التي يمكنها تحويل البيانات الصوتية الخام إلى ميزات الإدخال التي يتوقعها النموذج.

لذا ماذا يفعل مستخرج الميزات بالبيانات الصوتية الخام؟ دعنا نلقي نظرة على مستخرج ميزات [Whisper](https://huggingface.co/papers/2212.04356)
لفهم بعض تحويلات استخراج الميزات الشائعة. Whisper هو نموذج مسبق التدريب لل
الاعتراف التلقائي بالكلام (ASR) نشر في سبتمبر 2022 بواسطة Alec Radford et al. من OpenAI.

أولاً، يقوم مستخرج ميزات Whisper بملء/اقتطاع دفعة من الأمثلة الصوتية بحيث يكون
لجميع الأمثلة طول إدخال 30 ثانية. يتم ملء الأمثلة الأقصر من هذا إلى 30 ثانية عن طريق إضافة أصفار إلى نهاية
التسلسل (الأصفار في إشارة صوتية تقابل عدم وجود إشارة أو صمت). يتم اقتطاع الأمثلة الأطول من 30 ثانية إلى 30 ثانية.
نظرًا لأن جميع العناصر في الدفعة يتم ملؤها/اقتطاعها إلى طول أقصى في مساحة الإدخال، فلا توجد حاجة لقناع الاهتمام. Whisper فريد من نوعه في هذا الصدد، معظم النماذج الصوتية الأخرى تتطلب قناع اهتمام يفصل
حيث تم ملء التسلسلات، وبالتالي حيث يجب تجاهلها في آلية الاهتمام الذاتي. تم تدريب Whisper
للعمل بدون قناع اهتمام والاستنتاج مباشرة من إشارات الكلام حيث يتم تجاهل المدخلات.

العملية الثانية التي يقوم بها مستخرج ميزات Whisper هي تحويل المصفوفات الصوتية المملوءة إلى مخططات طيفية لوغاريتمية.
كما تتذكر، تصف هذه المخططات الطيفية كيفية تغير ترددات الإشارة بمرور الوقت، معبر عنها على مقياس ميل
وتقاس بالديسيبل (الجزء اللوغاريتمي) لجعل الترددات والسعات أكثر تمثيلاً للسمع البشري.

يمكن تطبيق جميع هذه التحويلات على بياناتك الصوتية الخام ببضع سطور من الكود. دعنا نستمر في التحميل
مستخرج الميزات من نقطة تفتيش Whisper المسبقة التدريب لتكون جاهزة لبياناتنا الصوتية:

```py
from transformers import WhisperFeatureExtractor

feature_extractor = WhisperFeatureExtractor.from_pretrained("openai/whisper-small")
```

بعد ذلك، يمكنك كتابة دالة لمعالجة مثال صوتي واحد عن طريق تمريره عبر `feature_extractor`.

```py
def prepare_dataset(example):
    audio = example["audio"]
    features = feature_extractor(
        audio["array"], sampling_rate=audio["sampling_rate"], padding=True
    )
    return features
```

يمكننا تطبيق دالة إعداد البيانات على جميع أمثلة التدريب الخاصة بنا باستخدام طريقة `map` في 🤗 Datasets:

```py
minds = minds.map(prepare_dataset)
minds
```

**الإخراج:**
```out
Dataset(
    {
        features: ["path", "audio", "transcription", "intent_class", "input_features"],
        num_rows: 624,
    }
)
بكل سهولة، أصبح لدينا الآن مخططات طيفية لوجية ميلية كـ `input_features` في مجموعة البيانات.

دعنا نصورها لأحد الأمثلة في مجموعة بيانات `minds`:

```py
import numpy as np

example = minds[0]
input_features = example["input_features"]

plt.figure().set_figwidth(12)
librosa.display.specshow(
    np.asarray(input_features[0]),
    x_axis="time",
    y_axis="mel",
    sr=feature_extractor.sampling_rate,
    hop_length=feature_extractor.hop_length,
)
plt.colorbar()
```

<div class="flex justify-center">
    <img src="https://huggingface.co/datasets/huggingface-course/audio-course-images/resolve/main/log_mel_whisper.png" alt="مخطط طيفي لوجي ميل">
</div>

الآن يمكنك رؤية شكل الإدخال الصوتي لنموذج Whisper بعد المعالجة المسبقة.

تتولى فئة مستخرج الميزات الخاصة بالنموذج مهمة تحويل البيانات الصوتية الخام إلى الصيغة التي يتوقعها النموذج. ومع ذلك،
تتطلب العديد من المهام التي تتضمن الصوت أن تكون متعددة الوسائط، على سبيل المثال، التعرف على الكلام. في مثل هذه الحالات، تقدم مكتبة 🤗 Transformers أيضًا محددات الرموز المخصصة للنموذج لمعالجة إدخالات النص. للحصول على نظرة متعمقة حول محددات الرموز، يرجى الرجوع إلى [دورة NLP](https://huggingface.co/course/chapter2/4).

يمكنك تحميل مستخرج الميزات ومحدد الرموز لنموذج Whisper وغيره من النماذج متعددة الوسائط بشكل منفصل، أو يمكنك تحميلهما معًا عبر
ما يسمى المعالج. لجعل الأمور أكثر بساطة، استخدم `AutoProcessor` لتحميل مستخرج ميزات النموذج والمعالج من
نقطة تفتيش، مثل هذا:

```py
from transformers import AutoProcessor

processor = AutoProcessor.from_pretrained("openai/whisper-small")
```

لقد قمنا هنا بتوضيح خطوات الإعداد الأساسية للبيانات. بالطبع، قد تتطلب البيانات المخصصة معالجة مسبقة أكثر تعقيدًا.
في هذه الحالة، يمكنك توسيع دالة `prepare_dataset` لأداء أي نوع من تحويلات البيانات المخصصة. مع 🤗 Datasets،
إذا كنت تستطيع كتابتها كدالة بايثون، يمكنك [تطبيقها](https://huggingface.co/docs/datasets/audio_process) على مجموعة بياناتك!