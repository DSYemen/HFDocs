# هندسات CTC

CTC أو التصنيف الزمني الاتصالي هي تقنية تُستخدم مع نماذج المحول المُشفر فقط للتعرف التلقائي على الكلام. أمثلة على هذه النماذج هي **Wav2Vec2**، و**HuBERT**، و**M-CTC-T**.

إن نموذج المحول المُشفر فقط هو أبسط أنواع المحول لأنه يستخدم فقط الجزء المُشفر من النموذج. يقوم المُشفر بقراءة تسلسل الإدخال (موجة الصوت) ويحول هذا إلى تسلسل من الحالات المخفية، والتي تُعرف أيضًا بالغرسات الإخراجية.

مع نموذج CTC، نطبق خريطة خطية إضافية على تسلسل الحالات المخفية للحصول على تنبؤات تسمية الفئة. تسميات الفئة هي **حروف الأبجدية** (أ، ب، ج، ...). بهذه الطريقة نتمكن من التنبؤ بأي كلمة في اللغة المستهدفة برأس تصنيف صغير، حيث أن المفردات تحتاج فقط إلى 26 حرفًا بالإضافة إلى بعض الرموز الخاصة.

<div class="flex justify-center">
    <img src="https://huggingface.co/datasets/huggingface-course/audio-course-images/resolve/main/wav2vec2-ctc.png" alt="محول المُشفر مع رأس CTC في الأعلى">
</div>

حتى الآن، هذا مشابه جدًا لما نفعله في NLP مع نموذج مثل BERT: نموذج محول المُشفر فقط يقوم برسم رموز النص الخاصة بنا إلى تسلسل من الحالات المخفية للمُشفر، ثم نطبق خريطة خطية للحصول على تنبؤ تسمية فئة واحدة لكل حالة مخفية.

هنا تكمن الصعوبة: في الكلام، لا نعرف **محاذاة** المدخلات الصوتية والمخرجات النصية. نحن نعلم أن الترتيب الذي يُنطق به الكلام هو نفسه الترتيب الذي يُكتب به النص (تُسمى المحاذاة أحادية النغمة)، لكننا لا نعرف كيف تتوافق الحروف في النص المكتوب مع الصوت. هنا يأتي دور خوارزمية CTC.

<Tip>
💡 في نماذج NLP تتكون المفردات عادة من آلاف الرموز التي تصف ليس فقط الحروف الفردية ولكن أجزاء من الكلمات أو حتى كلمات كاملة. ومع ذلك، بالنسبة لـ CTC، تعمل المفردات الصغيرة بشكل أفضل ونحاول عمومًا إبقائها أقل من 50 حرفًا. لا نهتم بحالة الأحرف، لذا فإن استخدام الأحرف الكبيرة فقط (أو الأحرف الصغيرة فقط) يكفي. تُكتب الأرقام، على سبيل المثال، "20" تصبح "twenty". بالإضافة إلى الأحرف، نحتاج إلى رمز فاصل الكلمات على الأقل (مسافة) ورمز الحشو. تمامًا كما في نموذج NLP، يسمح لنا رمز الحشو بدمج أمثلة متعددة في دفعة واحدة، ولكنه أيضًا الرمز الذي سيتنبأ به النموذج للصمت. في اللغة الإنجليزية، من المفيد أيضًا الاحتفاظ بالرمز `'` — فبعد كل شيء، "it's" و"its" لهما معانٍ مختلفة جدًا.
</Tip>

## يا رجل، أين محاذاتي؟

يتضمن التعرف التلقائي على الكلام أو ASR أخذ الصوت كإدخال وإنتاج نص كإخراج. لدينا بعض الخيارات للتنبؤ بالنص:

- كحروف فردية
- كمقاطع لفظية
- كرموز كلمات

يتم تدريب نموذج ASR على مجموعة بيانات تتكون من أزواج (الصوت، النص) حيث يكون النص نسخة بشرية من ملف الصوت. بشكل عام، لا تتضمن مجموعة البيانات أي معلومات توقيت تشير إلى الكلمة أو المقطع الذي يحدث في ملف الصوت. نظرًا لعدم إمكانية الاعتماد على معلومات التوقيت أثناء التدريب، ليس لدينا أي فكرة عن كيفية محاذاة تسلسل الإدخال والإخراج.

لنفترض أن إدخالنا هو ملف صوتي مدته ثانية واحدة. في **Wav2Vec2**، يقوم النموذج أولاً بتخفيض عينة الصوت باستخدام مُشفر ميزات CNN إلى تسلسل أقصر من الحالات المخفية، حيث يوجد متجه حالة مخفية واحد لكل 20 مللي ثانية من الصوت. بالنسبة لثانية واحدة من الصوت، نقوم بعد ذلك بإرسال تسلسل من 50 حالة مخفية إلى مُشفر المحول. (تتداخل مقاطع الصوت المستخرجة من تسلسل الإدخال جزئيًا، لذا على الرغم من إصدار متجه حالة مخفية واحد كل 20 مللي ثانية، يمثل كل متجه حالة مخفية 25 مللي ثانية من الصوت).

يتنبأ مُشفر المحول بتمثيل ميزة واحدة لكل من هذه الحالات المخفية، مما يعني أننا نتلقى تسلسلًا من 50 إخراجًا من المحول. لكل من هذه الإخراجات أبعاد 768. وبالتالي، فإن تسلسل الإخراج لمُشفر المحول في هذا المثال له شكل (768، 50). نظرًا لأن كل من هذه التنبؤات يغطي 25 مللي ثانية من الوقت، وهو أقصر من مدة المقطع اللفظي، فمن المنطقي التنبؤ بمقاطع لفظية أو حروف فردية ولكن ليس كلمات كاملة. تعمل CTC بشكل أفضل مع مفردات صغيرة، لذا سنتنبأ بالحروف.

<div class="flex justify-center">
    <img src="https://huggingface.co/datasets/huggingface-course/audio-course-images/resolve/main/cnn-feature-encoder.png" alt="موجة الصوت يتم تحويلها إلى تسلسل أقصر من الحالات المخفية">
</div>

لإجراء تنبؤات النص، نقوم برسم كل من مخرجات المُشفر ذات الأبعاد 768 إلى تسميات الحروف الخاصة بنا باستخدام طبقة خطية (رأس "CTC"). بعد ذلك، يتنبأ النموذج بمجموعة من (50، 32) تحتوي على اللوغاريتمات، حيث 32 هو عدد الرموز في المفردات. نظرًا لأننا نجري تنبؤًا واحدًا لكل من الميزات في التسلسل، فإننا ننتهي بـ 50 تنبؤًا بالحروف لكل ثانية من الصوت.

ومع ذلك، إذا تنبأنا ببساطة بحرف واحد كل 20 مللي ثانية، فقد يبدو تسلسل الإخراج الخاص بنا شيئًا مثل هذا:

```text
BRIIONSAWWSOMEETHINGCLOSETOPANICONHHISOPPONENT'SSFAACEWHENTHEMANNFINALLLYRREECOGGNNIIZEDHHISSERRRRORR ...
```

إذا نظرت عن كثب، فهو يشبه اللغة الإنجليزية إلى حد ما ولكن تم تكرار العديد من الحروف. وذلك لأن النموذج يحتاج إلى إخراج *شيء ما* لكل 20 مللي ثانية من الصوت في تسلسل الإدخال، وإذا تم توزيع حرف على فترة أطول من 20 مللي ثانية، فسيظهر عدة مرات في الإخراج. لا توجد طريقة لتجنب ذلك، خاصةً لأننا لا نعرف توقيت النص المكتوب أثناء التدريب. CTC هي طريقة لتصفية هذه المكررات.

(في الواقع، يحتوي التسلسل المتوقع أيضًا على الكثير من رموز الحشو عندما لا يكون النموذج متأكدًا تمامًا مما يمثله الصوت، أو للمساحة الفارغة بين الحروف. قمنا بإزالة رموز الحشو هذه من المثال للتوضيح. التداخل الجزئي بين مقاطع الصوت هو سبب آخر لتكرار الحروف في الإخراج).

## خوارزمية CTC

مفتاح خوارزمية CTC هو استخدام رمز خاص، غالبًا ما يُسمى **رمز فارغ**. هذا مجرد رمز آخر سيتنبأ به النموذج وهو جزء من المفردات. في هذا المثال، يظهر الرمز الفارغ على أنه `_`. يخدم هذا الرمز الخاص كحد صارم بين مجموعات الحروف.

قد يكون الإخراج الكامل من نموذج CTC شيئًا مثل ما يلي:

```text
B_R_II_O_N_||_S_AWW_|||||_S_OMEE_TH_ING_||_C_L_O_S_E||TO|_P_A_N_I_C_||_ON||HHI_S||_OP_P_O_N_EN_T_'SS||_F_AA_C_E||_W_H_EN||THE||M_A_NN_||||_F_I_N_AL_LL_Y||||_RREE_C_O_GG_NN_II_Z_ED|||HHISS|||_ER_RRR_ORR||||
```

الرمز `|` هو رمز فاصل الكلمات. في المثال، نستخدم `|` بدلاً من مسافة مما يجعل من السهل اكتشاف مكان فواصل الكلمات، ولكنه يخدم نفس الغرض.

يجعل الرمز الفارغ لـ CTC من الممكن تصفية الحروف المكررة. على سبيل المثال، دعنا ننظر إلى الكلمة الأخيرة من التسلسل المتوقع، `_ER_RRR_ORR`. بدون الرمز الفارغ لـ CTC، بدت الكلمة هكذا:

```text
ERRRRORR
```

إذا كنا سنزيل ببساطة الحروف المكررة، فسيصبح هذا `EROR`. من الواضح أن هذا ليس التهجئة الصحيحة. ولكن مع الرمز الفارغ لـ CTC يمكننا إزالة المكررات في كل مجموعة، بحيث:

```text
_ER_RRR_ORR
```

يصبح:

```text
_ER_R_OR
```

والآن نزيل الرمز الفارغ `_` للحصول على الكلمة النهائية:

```text
ERROR
```

إذا طبقنا هذه المنطقية على النص بأكمله، بما في ذلك `|`، واستبدلنا رموز `|` الباقية بمسافات، فإن الإخراج النهائي الذي تم فك تشفيره بواسطة CTC هو:

```text
BRION SAW SOMETHING CLOSE TO PANIC ON HIS OPPONENT'S FACE WHEN THE MAN FINALLY RECOGNIZED HIS ERROR
للتذكير، فإن النموذج يتنبأ برمز واحد (حرف) لكل 20 مللي ثانية من الصوت (المتداخل جزئيًا) من الموجة الصوتية المدخلة. هذا يعطي الكثير من المكررات. وبفضل رمز CTC الفارغ، يمكننا بسهولة إزالة هذه المكررات دون تدمير التهجئة الصحيحة للكلمات. هذه طريقة بسيطة وملائمة للغاية لحل مشكلة محاذاة النص الناتج مع الصوت المدخل.

<نصيحة>
💡 في نموذج Wav2Vec2 الفعلي، يكون رمز CTC الفارغ هو نفسه رمز الحشو `<pad>`. سوف يتنبأ النموذج بالعديد من رموز `<pad>` هذه، على سبيل المثال عندما لا يكون هناك حرف واضح للتنبؤ به لـ 20 مللي ثانية الحالية من الصوت. استخدام نفس الرمز للحشو وللإلغاء باستخدام CTC يبسط خوارزمية فك التشفير ويساعد على إبقاء المفردات صغيرة.
</نصيحة>

إضافة CTC إلى نموذج محول الترميز سهل: تتجه سلسلة الإخراج من محول الترميز إلى طبقة خطية تقوم بتصوير الميزات الصوتية إلى المفردات. يتم تدريب النموذج بخسارة CTC خاصة.

أحد عيوب CTC هو أنه قد ينتج كلمات تبدو صحيحة، ولكنها غير مكتوبة بشكل صحيح. ففي النهاية، رأس CTC لا ينظر إلا إلى الحروف الفردية، وليس الكلمات الكاملة. إحدى طرق تحسين جودة النسخ الصوتي هي استخدام نموذج لغة خارجي. هذا النموذج اللغوي يعمل بشكل أساسي كمدقق إملائي فوق ناتج CTC.

## ما الفرق بين Wav2Vec2 وHuBERT وM-CTC-T، ...؟

جميع نماذج CTC القائمة على المحول لها بنية معمارية متشابهة جدًا: فهي تستخدم محول الترميز (ولكن ليس فك الترميز) مع رأس CTC في الأعلى. من ناحية البنية المعمارية، فهي أكثر تشابهًا من الاختلاف.

أحد الاختلافات بين Wav2Vec2 وM-CTC-T هو أن الأول يعمل على موجات صوتية خام بينما يستخدم الأخير مخططات طيفية ميلية كمدخلات. كما تم تدريب النماذج لأغراض مختلفة. فعلى سبيل المثال، تم تدريب M-CTC-T على التعرف على الكلام متعدد اللغات، وبالتالي لديه رأس CTC كبير نسبيًا يشمل الحروف الصينية بالإضافة إلى الأبجديات الأخرى.

يستخدم Wav2Vec2 وHuBERT نفس البنية المعمارية تمامًا ولكن يتم تدريبهما بطرق مختلفة للغاية. يتم تدريب Wav2Vec2 مسبقًا مثل نمذجة اللغة المقنعة لBERT، من خلال التنبؤ بوحدات الكلام للأجزاء المقنعة من الصوت. يأخذ HuBERT إلهام BERT خطوة أبعد ويتعلم التنبؤ "بوحدات الكلام المنفصلة"، والتي تشبه الرموز في جملة نصية، بحيث يمكن معالجة الكلام باستخدام تقنيات NLP المعمول بها.

لتوضيح الأمر، فإن النماذج المميزة هنا ليست النماذج الوحيدة القائمة على المحول والتي تستخدم CTC. هناك العديد من النماذج الأخرى، ولكن الآن تعرف أنهم جميعًا يعملون بطريقة مماثلة.