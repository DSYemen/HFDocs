# النماذج المُدربة مسبقًا ومجموعات البيانات لتصنيف الصوت

يستضيف مركز هاجينج فيس هاب أكثر من 500 نموذج مُدرب مسبقًا لتصنيف الصوت. في هذا القسم، سنمر عبر بعض مهام تصنيف الصوت الشائعة ونقترح نماذج مُدربة مسبقًا مناسبة لكل منها. باستخدام فئة `pipeline()`، يعد التبديل بين النماذج والمهام أمرًا بسيطًا - بمجرد معرفة كيفية استخدام `pipeline()` لنموذج واحد، ستتمكن من استخدامه لأي نموذج على المركز دون تغيير الرمز! وهذا يجعل التجربة مع فئة `pipeline()` سريعة للغاية، مما يتيح لك اختيار أفضل نموذج مُدرب مسبقًا لاحتياجاتك بسرعة.

قبل أن ننتقل إلى مشاكل تصنيف الصوت المختلفة، دعنا نستعرض بسرعة هندسة المحول المستخدمة عادة. يتم تحفيز هندسة تصنيف الصوت القياسية بطبيعة المهمة؛ نريد تحويل تسلسل إدخالات الصوت (أي مصفوفة إدخال الصوت لدينا) إلى تنبؤ تسمية فئة واحدة. تقوم النماذج التي تستخدم المُشفر فقط أولاً بتخطيط تسلسل إدخال الصوت إلى تسلسل من تمثيلات الحالة المخفية عن طريق تمرير الإدخالات عبر كتلة محول. ثم يتم رسم تسلسل تمثيلات الحالة المخفية إلى تسمية فئة الإخراج عن طريق أخذ المتوسط على الحالات المخفية، وتمرير المتجه الناتج عبر طبقة التصنيف الخطية. وبالتالي، هناك تفضيل للنماذج التي تستخدم المُشفر فقط لتصنيف الصوت.

تُدخل النماذج التي تستخدم المُفكك فقط تعقيدًا غير ضروري للمهمة، حيث تفترض أن المخرجات يمكن أن تكون أيضًا تسلسلًا من التنبؤات (بدلاً من تنبؤ تسمية فئة واحدة)، وبالتالي تُنتج مخرجات متعددة. لذلك، لديهم سرعة استنتاج أبطأ ولا يتم استخدامها عادةً. يتم إهمال نماذج المُشفر-المُفكك إلى حد كبير لنفس السبب. هذه الخيارات المعمارية مماثلة لتلك الموجودة في NLP، حيث يتم تفضيل النماذج التي تستخدم المُشفر فقط مثل [BERT](https://huggingface.co/blog/bert-101) لمهام تصنيف التسلسل، ويتم حجز النماذج التي تستخدم المُفكك فقط مثل GPT لمهام توليد التسلسل.

الآن بعد أن استعرضنا هندسة المحول القياسية لتصنيف الصوت، دعنا ننتقل إلى المجموعات الفرعية المختلفة لتصنيف الصوت ونغطي النماذج الأكثر شعبية!

## تثبيت المحولات 🤗

في وقت كتابة هذا التقرير، أحدث التحديثات المطلوبة لخط أنابيب تصنيف الصوت موجودة فقط في الإصدار `main` من مستودع المحولات 🤗، بدلاً من أحدث إصدار من PyPi. للتأكد من أن لدينا هذه التحديثات محليًا، سنقوم بتثبيت المحولات من فرع `main` باستخدام الأمر التالي:

```
pip install git+https://github.com/huggingface/transformers
```

## اكتشاف الكلمات الرئيسية

اكتشاف الكلمات الرئيسية (KWS) هي مهمة تحديد كلمة رئيسية في عبارة منطوقة. تشكل مجموعة الكلمات الرئيسية المحتملة مجموعة تسميات الفئات المتوقعة. وبالتالي، لاستخدام نموذج اكتشاف الكلمات الرئيسية المُدرب مسبقًا، يجب التأكد من أن كلماتك الرئيسية تتطابق مع تلك التي تم تدريب النموذج عليها مسبقًا. فيما يلي، سنقدم مجموعتين من البيانات ونموذجين لاكتشاف الكلمات الرئيسية.

### مايندس-14

دعنا نستخدم نفس مجموعة بيانات [MINDS-14](https://huggingface.co/datasets/PolyAI/minds14) التي قمت باستكشافها في الوحدة السابقة. إذا تذكرت، تحتوي MINDS-14 على تسجيلات لأشخاص يطرحون أسئلة على نظام مصرفي إلكتروني بعدة لغات ولهجات، ولديها فئة `intent_class` لكل تسجيل. يمكننا تصنيف التسجيلات حسب نية المكالمة.

```python
from datasets import load_dataset

minds = load_dataset("PolyAI/minds14", name="en-AU", split="train")
```

سنقوم بتحميل نقطة التفتيش [`"anton-l/xtreme_s_xlsr_300m_minds14"`](https://huggingface.co/anton-l/xtreme_s_xlsr_300m_minds14)، وهي نموذج XLS-R مُدرب على MINDS-14 لحوالي 50 حقبة. يحقق دقة 90% عبر جميع اللغات من MINDS-14 على مجموعة التقييم.

```python
from transformers import pipeline

classifier = pipeline(
    "audio-classification",
    model="anton-l/xtreme_s_xlsr_300m_minds14",
)
```

أخيرًا، يمكننا تمرير عينة إلى خط أنابيب التصنيف لإجراء تنبؤ:
```python
classifier(minds[0]["audio"])
```
**الناتج:**
```
[
    {"score": 0.9631525278091431, "label": "pay_bill"},
    {"score": 0.02819698303937912, "label": "freeze"},
    {"score": 0.0032787492964416742, "label": "card_issues"},
    {"score": 0.0019414445850998163, "label": "abroad"},
    {"score": 0.0008378693601116538, "label": "high_value_payment"},
]
```

رائع! لقد حددنا أن نية المكالمة كانت دفع فاتورة، باحتمالية 96%. يمكنك تخيل استخدام هذا النوع من أنظمة اكتشاف الكلمات الرئيسية كمرحلة أولى لمركز اتصال آلي، حيث نريد تصنيف مكالمات العملاء الواردة بناءً على استفساراتهم وتقديم الدعم السياقي لهم وفقًا لذلك.

### أوامر الكلام

أوامر الكلام هي مجموعة بيانات من الكلمات المنطوقة مصممة لتقييم نماذج تصنيف الصوت على كلمات الأوامر البسيطة. تتكون مجموعة البيانات من 15 فئة من الكلمات الرئيسية، وفئة للصمت، وفئة مجهولة لإدراج الإيجابي الكاذب. الكلمات الرئيسية الـ 15 هي كلمات مفردة يتم استخدامها عادةً في إعدادات الأجهزة للتحكم في المهام الأساسية أو إطلاق عمليات أخرى.

يتم تشغيل نموذج مشابه بشكل مستمر على هاتفك المحمول. هنا، بدلاً من وجود كلمات أوامر مفردة، لدينا كلمات "تنبيه" محددة لجهازك، مثل "Hey Google" أو "Hey Siri". عندما يكتشف نموذج تصنيف الصوت هذه الكلمات، فإنه يطلق هاتفك لبدء الاستماع إلى الميكروفون وتدوين كلامك باستخدام نموذج التعرف على الكلام.

نموذج تصنيف الصوت أصغر وأخف بكثير من نموذج التعرف على الكلام، وغالبًا ما يحتوي على عدة ملايين من المعلمات مقارنة بعدة مئات من الملايين للتعرف على الكلام. وبالتالي، يمكن تشغيله باستمرار على جهازك دون استنزاف البطارية! فقط عندما يتم اكتشاف كلمة التنبيه، يتم إطلاق نموذج التعرف على الكلام الأكبر، ثم يتم إغلاقه مرة أخرى. سنغطي نماذج المحول للتعرف على الكلام في الوحدة التالية، لذا بحلول نهاية الدورة التدريبية، يجب أن تكون لديك الأدوات التي تحتاجها لبناء مساعدك الصوتي الخاص!

كما هو الحال مع أي مجموعة بيانات على مركز هاجينج فيس هاب، يمكننا الحصول على فكرة عن نوع بيانات الصوت الموجودة دون تنزيلها أو حفظها في الذاكرة. بعد التوجه إلى [بطاقة مجموعة بيانات أوامر الكلام](https://huggingface.co/datasets/speech_commands) على المركز، يمكننا استخدام عارض مجموعة البيانات للتنقل خلال أول 100 عينة من مجموعة البيانات، والاستماع إلى ملفات الصوت والتحقق من أي معلومات أخرى خاصة بالبيانات الوصفية:

<div class="flex justify-center">
     <img src="https://huggingface.co/datasets/huggingface-course/audio-course-images/resolve/main/speech_commands.png" alt="مخطط عارض مجموعة البيانات.">
 </div>

تعد معاينة مجموعة البيانات طريقة رائعة لتجربة مجموعات بيانات الصوت قبل الالتزام باستخدامها. يمكنك اختيار أي مجموعة بيانات على المركز، والتنقل خلال العينات والاستماع إلى الصوت لمختلف المجموعات الفرعية والتقسيمات، وتقييم ما إذا كانت مجموعة البيانات مناسبة لاحتياجاتك. بمجرد اختيار مجموعة بيانات، يكون تحميل البيانات أمرًا بسيطًا بحيث يمكنك البدء في استخدامها.

دعنا نفعل ذلك بالضبط ونحمل عينة من مجموعة بيانات أوامر الكلام باستخدام وضع البث:

```python
speech_commands = load_dataset(
    "speech_commands", "v0.02", split="validation", streaming=True
)
sample = next(iter(speech_commands))
```

سنقوم بتحميل نقطة تفتيش رسم بياني طيفي صوتي رسمية [Audio Spectrogram Transformer](https://huggingface.co/docs/transformers/model_doc/audio-spectrogram-transformer)
مُدربة على مجموعة بيانات أوامر الكلام، تحت مساحة الاسم [`"MIT/ast-finetuned-speech-commands-v2"`](https://huggingface.co/MIT/ast-finetuned-speech-commands-v2):

```python
classifier = pipeline(
    "audio-classification", model="MIT/ast-finetuned-speech-commands-v2"
)
classifier(sample["audio"].copy())
```
**الناتج:**
```
[{'score': 0.9999892711639404, 'label': 'backward'},
 {'score': 1.7504888774055871e-06, 'label': 'happy'},
 {'score': 6.703040185129794e-07, 'label': 'follow'},
 {'score': 5.805884484288981e-07, 'label': 'stop'},
 {'score': 5.614546694232558e-07, 'label': 'up'}]
```

رائع! يبدو أن المثال يحتوي على كلمة "backward" باحتمالية عالية. يمكننا الاستماع إلى العينة
وتحقق من صحة ذلك:
```
from IPython.display import Audio

Audio(sample["audio"]["array"], rate=sample["audio"]["sampling_rate"])
```

الآن، قد تتساءل عن كيفية اختيارنا لهذه النماذج المُدربة مسبقًا لنعرضها لك في هذه الأمثلة لتصنيف الصوت.
في الواقع، إن إيجاد نماذج مُدربة مسبقًا لمجموعة بياناتك ومهمتك أمر سهل للغاية! أول شيء نحتاج إلى فعله
هو التوجه إلى Hugging Face Hub والنقر على علامة التبويب "نماذج": https://huggingface.co/models

هذا سيُظهر جميع النماذج على Hugging Face Hub، مرتبة حسب عدد مرات التحميل في آخر 30 يومًا:

<div class="flex justify-center">
     <img src="https://huggingface.co/datasets/huggingface-course/audio-course-images/resolve/main/all_models.png">
 </div>

ستلاحظ على الجانب الأيسر أن لدينا مجموعة من علامات التبويب التي يمكننا اختيارها لتصفية النماذج حسب المهمة، المكتبة،
مجموعة البيانات، إلخ. قم بالتمرير لأسفل وحدد المهمة "تصنيف الصوت" من قائمة مهام الصوت:

<div class="flex justify-center">
     <img src="https://huggingface.co/datasets/huggingface-course/audio-course-images/resolve/main/by_audio_classification.png">
 </div>

الآن، نُعرض على مجموعة فرعية من 500+ نموذج لتصنيف الصوت على Hub. لزيادة تحسين هذا الاختيار، يمكننا
تصفية النماذج حسب مجموعة البيانات. انقر على علامة التبويب "مجموعات البيانات"، وفي مربع البحث اكتب "speech_commands". بينما تبدأ بالكتابة،
سترى اختيار `speech_commands` يظهر أسفل علامة تبويب البحث. يمكنك النقر على هذا الزر لتصفية جميع
نماذج تصنيف الصوت إلى تلك المُدربة على مجموعة بيانات Speech Commands:

<div class="flex justify-center">
     <img src="https://huggingface.co/datasets/huggingface-course/audio-course-images/resolve/main/by_speech_commands.png">
 </div>

رائع! نرى أن لدينا 6 نماذج مُدربة مسبقًا متاحة لنا لهذه المجموعة المحددة من البيانات والمهمة. ستتعرف على
أول هذه النماذج كنقطة تفتيش محول طيف الصوت التي استخدمناها في المثال السابق. هذه العملية
لتصفية النماذج على Hub هي بالضبط كيف قمنا باختيار نقطة التفتيش لنعرضها لك!

## تحديد اللغة

تحديد اللغة (LID) هي مهمة تحديد اللغة المنطوقة في عينة صوتية من قائمة لغات مرشحة. يمكن أن يشكل LID جزءًا مهمًا في العديد من خطوط أنابيب الكلام. على سبيل المثال، بالنظر إلى عينة صوتية بلغة غير معروفة،
يمكن استخدام نموذج LID لتصنيف اللغة (اللغات) المنطوقة في العينة الصوتية، ثم اختيار نموذج التعرف على الكلام المناسب المدرب على تلك اللغة لنسخ الصوت.

### FLEURS

FLEURS (تقييم التعلم القليل للتمثيلات الشاملة للكلام) هي مجموعة بيانات لتقييم أنظمة التعرف على الكلام
في 102 لغة، بما في ذلك العديد من اللغات المصنفة على أنها 'قليلة الموارد'. ألق نظرة على بطاقة مجموعة بيانات FLEURS
على Hub واستكشف اللغات المختلفة الموجودة: [google/fleurs](https://huggingface.co/datasets/google/fleurs).
هل يمكنك العثور على لغتك الأم هنا؟ إذا لم يكن الأمر كذلك، فما هي اللغة الأقرب صلة؟

دعنا نحمّل عينة من الانقسام التحقق من صحة مجموعة بيانات FLEURS باستخدام وضع البث:

```python
fleurs = load_dataset("google/fleurs", "all", split="validation", streaming=True)
sample = next(iter(fleurs))
```

رائع! الآن يمكننا تحميل نموذج تصنيف الصوت الخاص بنا. لهذا، سنستخدم نسخة من [Whisper](https://arxiv.org/pdf/2212.04356.pdf)
المُدربة على مجموعة بيانات FLEURS، والتي تعد حاليًا أكثر نماذج LID أداءً على Hub:

```python
classifier = pipeline(
    "audio-classification", model="sanchit-gandhi/whisper-medium-fleurs-lang-id"
)
```

بعد ذلك يمكننا تمرير الصوت عبر المصنف لدينا وتوليد تنبؤ:
```python
classifier(sample["audio"])
```
**الناتج:**
```
[{'score': 0.9999330043792725, 'label': 'Afrikaans'},
 {'score': 7.093023668858223e-06, 'label': 'Northern-Sotho'},
 {'score': 4.269149485480739e-06, 'label': 'Icelandic'},
 {'score': 3.2661141631251667e-06, 'label': 'Danish'},
 {'score': 3.2580724109720904e-06, 'label': 'Cantonese Chinese'}]
```

يمكننا أن نرى أن النموذج تنبأ بأن الصوت كان باللغة الأفريكانية باحتمالية عالية للغاية (قريبة من 1). تحتوي مجموعة بيانات FLEURS
على بيانات صوتية من مجموعة واسعة من اللغات - يمكننا أن نرى أن تسميات الفئة الممكنة تشمل Northern-Sotho، Icelandic،
Danish و Cantonese Chinese من بين لغات أخرى. يمكنك العثور على القائمة الكاملة للغات على بطاقة مجموعة البيانات هنا: [google/fleurs](https://huggingface.co/datasets/google/fleurs).

الآن دورك! ما هي نقاط التفتيش الأخرى التي يمكنك العثور عليها لـ FLEURS LID على Hub؟ ما هي نماذج المحول التي يستخدمونها تحت الغطاء؟

## تصنيف الصوت بدون بيانات تدريبية

في النموذج التقليدي لتصنيف الصوت، يتنبأ النموذج بتسمية الفئة من مجموعة مُعرفة مسبقًا من
الفئات الممكنة. هذا يضع حاجزًا أمام استخدام النماذج المُدربة مسبقًا لتصنيف الصوت، حيث يجب أن تتطابق مجموعة تسميات النموذج المُدرب مسبقًا مع مجموعة تسميات المهمة النهائية. بالنسبة للمثال السابق لـ LID، يجب على النموذج التنبؤ بواحدة من
فئات اللغات 102 التي تم تدريبه عليها. إذا كانت المهمة النهائية تتطلب في الواقع 110 لغات، فلن يتمكن النموذج من التنبؤ بـ 8 من 110 لغات، وبالتالي سيتطلب إعادة التدريب لتحقيق التغطية الكاملة. هذا يحد من
فعالية التعلم التحويلي لمهام تصنيف الصوت.

تصنيف الصوت بدون بيانات تدريبية هي طريقة لأخذ نموذج تصنيف صوت مُدرب مسبقًا مُدرب على مجموعة من الأمثلة المُعرفة
وتمكينه من تصنيف أمثلة جديدة من فئات غير معروفة مسبقًا. دعنا نلقي نظرة على كيفية تحقيق ذلك!

حاليًا، يدعم 🤗 Transformers نوعًا واحدًا من النماذج لتصنيف الصوت بدون بيانات تدريبية: نموذج [CLAP](https://huggingface.co/docs/transformers/model_doc/clap).
CLAP هو نموذج قائم على المحول يأخذ كلًا من الصوت والنص كمدخلات، ويحسب _التشابه_ بين الاثنين.
إذا مررنا مدخلاً نصيًا يرتبط ارتباطًا قويًا بمدخلاً صوتيًا، فسنحصل على درجة تشابه عالية. على العكس، تمرير
مدخلاً نصيًا لا علاقة له بالمدخلاً الصوتي سيعيد تشابهًا منخفضًا.

يمكننا استخدام هذا التنبؤ بالتشابه لتصنيف الصوت بدون بيانات تدريبية عن طريق تمرير مدخلاً صوتيًا واحدًا إلى النموذج و
تسميات مرشحة متعددة. سيعيد النموذج درجة تشابه لكل من التسميات المرشحة، ويمكننا اختيار
التسمية التي لديها أعلى درجة كتنبؤنا.

دعنا نأخذ مثالاً حيث نستخدم مدخلاً صوتيًا واحدًا من [Environmental Speech Challenge (ESC)](https://huggingface.co/datasets/ashraq/esc50)
مجموعة البيانات:

```python
dataset = load_dataset("ashraq/esc50", split="train", streaming=True)
audio_sample = next(iter(dataset))["audio"]["array"]
```

بعد ذلك نُعّرف التسميات المرشحة، والتي تشكل مجموعة تسميات التصنيف الممكنة. سيعيد النموذج
احتمالية التصنيف لكل من التسميات التي نُعّرفها. هذا يعني أننا نحتاج إلى معرفة _مسبقًا_ مجموعة التسميات الممكنة
في مشكلة التصنيف الخاصة بنا، بحيث تكون التسمية الصحيحة موجودة ضمن المجموعة وبالتالي يتم تعيين
احتمالية تسجيل صالحة. لاحظ أنه يمكننا إما تمرير المجموعة الكاملة من التسميات إلى النموذج، أو مجموعة فرعية مختارة يدويًا
نعتقد أنها تحتوي على التسمية الصحيحة. تمرير المجموعة الكاملة من التسميات سيكون أكثر شمولًا، ولكن يأتي
على حساب دقة تصنيف أقل حيث أن مساحة التصنيف أكبر (شريطة أن تكون التسمية الصحيحة هي مجموعة التسميات الفرعية التي اخترناها):

```python
candidate_labels = ["Sound of a dog", "Sound of vacuum cleaner"]
```

يمكننا تشغيل كل من المدخلات والنموذج لإيجاد التسمية المرشحة التي هي _الأكثر تشابهًا_ مع المدخلاً الصوتي:

```python
classifier = pipeline(
    task="zero-shot-audio-classification", model="laion/clap-htsat-unfused"
)
classifier(audio_sample, candidate_labels=candidate_labels)
```
**الناتج:**
```
[{'score': 0.9997242093086243, 'label': 'Sound of a dog'}, {'score': 0.0002758323971647769, 'label': 'Sound of vacuum cleaner'}]
```

حسنًا! يبدو أن النموذج واثق جدًا من أن لدينا صوت كلب - فهو يتنبأ به باحتمالية 99.96%، لذلك سنأخذ
ذلك كتنبؤنا. دعنا نتأكد مما إذا كنا على صواب عن طريق الاستماع إلى العينة الصوتية (لا ترفع صوتك عاليًا جدًا وإلا فقد تحصل على قفزة!):

```python
Audio(audio_sample, rate=16000)
```
رائع! لدينا صوت نباح كلب 🐕، وهو ما يتطابق مع تنبؤ النموذج. جرب استخدام عينات صوتية مختلفة
وتصنيفات مرشحة مختلفة - هل يمكنك تحديد مجموعة من التصنيفات التي تعطي تعميما جيدا عبر مجموعة بيانات ESC
؟ تلميح: فكر في المكان الذي يمكنك فيه العثور على معلومات حول الأصوات المحتملة في ESC وقم ببناء تصنيفاتك وفقا لذلك!

قد تتساءل لماذا لا نستخدم خط أنابيب التصنيف الصوتي ذو التصويب الصفري لجميع مهام التصنيف الصوتي؟
يبدو أنه يمكننا إجراء تنبؤات لأي مشكلة تصنيف صوتي من خلال تحديد تصنيفات الفئة المناسبة مسبقًا،
وبالتالي تجاوز القيد الذي يتطلب أن تتطابق مهمة التصنيف الخاصة بنا مع التصنيفات التي تم تدريب النموذج عليها مسبقًا.
هذا يرجع إلى طبيعة نموذج CLAP المستخدم في خط أنابيب التصويب الصفري: CLAP مُدرب مسبقًا على بيانات تصنيف صوتية _عامة_،
مماثلة للأصوات البيئية في مجموعة بيانات ESC، بدلاً من بيانات الكلام المحددة، مثل
التي كانت لدينا في مهمة LID. إذا أعطيته كلامًا باللغة الإنجليزية وكلامًا باللغة الإسبانية، فسيتمكن CLAP من معرفة أن كلا المثالين كانا
بيانات كلامية 🗣️ لكنه لن يتمكن من التمييز بين اللغات بنفس الطريقة التي يمكن لنموذج LID المخصص القيام بها.

## ما هي الخطوة التالية؟

لقد غطينا عددًا من مهام التصنيف الصوتي المختلفة وقمنا بعرض مجموعات البيانات والنماذج الأكثر ملاءمة التي
يمكنك تنزيلها من Hugging Face Hub واستخدامها في بضع سطور من التعليمات البرمجية باستخدام فئة `pipeline()`. تضمنت هذه المهام
اكتشاف الكلمات الرئيسية، وتحديد اللغة، والتصنيف الصوتي ذو التصويب الصفري.

ولكن ماذا لو أردنا القيام بشيء **جديد**؟ لقد عملنا بشكل مكثف على مهام معالجة الكلام، ولكن هذا مجرد جانب واحد
من التصنيف الصوتي. هناك مجال شائع آخر لمعالجة الصوت يتضمن **الموسيقى**. على الرغم من أن الموسيقى لها ميزات مختلفة متأصلة في الكلام،
يمكن تطبيق العديد من نفس المبادئ التي تعلمناها بالفعل على الموسيقى.

في القسم التالي، سنقوم بالمرور خطوة بخطوة على كيفية ضبط نموذج المحول باستخدام 🤗
المحولات على مهمة تصنيف الموسيقى. في نهاية هذا القسم، سيكون لديك نقطة ضبط دقيقة يمكنك توصيلها
في فئة `pipeline()`، مما يتيح لك تصنيف الأغاني بنفس الطريقة التي صنفنا بها الكلام هنا!