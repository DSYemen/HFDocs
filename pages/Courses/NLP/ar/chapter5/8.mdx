# End-of-chapter quiz

غطى هذا الفصل الكثير من الجوانب! لا تقلق إذا لم تستوعب كل التفاصيل؛ ستساعدك الفصول التالية على فهم كيفية عمل الأشياء خلف الكواليس.

ومع ذلك، قبل الانتقال إلى الفصل التالي، دعونا نختبر ما تعلمته في هذا الفصل.

### 1. تسمح لك دالة `load_dataset()` في 🤗 Datasets بتحميل مجموعة بيانات من أي من المواقع التالية؟

**الإجابة:** محليا، على جهاز الكمبيوتر المحمول الخاص بك، أو من Hugging Face Hub، أو من خادم بعيد.

### 2. دعنا نفترض أنك قمت بتحميل إحدى مهام GLUE على النحو التالي:

```py
from datasets import load_dataset

dataset = load_dataset("glue", "mrpc", split="train")
```

ما هي الأوامر التالية التي ستنتج عينة عشوائية مكونة من 50 عنصرًا من `dataset`؟

**الإجابة:** `dataset.shuffle().select(range(50))`

### 3. دعنا نفترض أن لديك مجموعة بيانات حول حيوانات أليفة تسمى `pets_dataset`، والتي تحتوي على عمود `name` يمثل اسم كل حيوان أليف. ما هي الطريقة التي يمكنك استخدامها لتصفية مجموعة البيانات بحيث تحتوي فقط على الحيوانات الأليفة التي تبدأ أسماؤها بحرف "L"؟

**الإجابة:** يمكنك استخدام طريقة `filter` مع دالة لامدا: `pets_dataset.filter(lambda x: x['name'].startswith('L'))`، أو إنشاء دالة تصفية صريحة وتمريرها إلى طريقة `filter`:

```py
def filter_names(x):
    return x['name'].startswith('L')

pets_dataset.filter(filter_names)
```

### 4. ما هو الربط بالذاكرة (Memory Mapping)؟

**الإجابة:** هو ربط بين ذاكرة الوصول العشوائي (RAM) والتخزين على نظام الملفات.

### 5. ما هي الفوائد الرئيسية للربط بالذاكرة؟

**الإجابة:**

1. الوصول إلى الملفات المرتبطة بالذاكرة أسرع من القراءة من القرص أو الكتابة عليه.
2. يمكن للتطبيقات الوصول إلى مقاطع من البيانات في ملف كبير للغاية دون الحاجة إلى قراءة الملف بالكامل في ذاكرة الوصول العشوائي أولاً.

### 6. لماذا تفشل الشفرة البرمجية التالية؟

```py
from datasets import load_dataset

dataset = load_dataset("allocine", streaming=True, split="train")
dataset[0]
```

**الإجابة:** تحاول الشفرة البرمجية الوصول إلى عنصر في `IterableDataset`.

### 7. ما هي الفوائد الرئيسية لإنشاء بطاقة مجموعة بيانات (Dataset Card)؟

**الإجابة:**

1. توفر معلومات عن الاستخدام المقصود ومهمات مجموعة البيانات المدعومة، مما يسمح لأعضاء المجتمع باتخاذ قرار مستنير حول استخدامها.
2. تساعد في تسليط الضوء على التحيزات الموجودة في مجموعة البيانات.
3. تحسن فرص استخدام الآخرين في المجتمع لمجموعة البيانات الخاصة بك.

### 8. ما هي عملية البحث الدلالية (Semantic Search)؟

**الإجابة:** هي طريقة للبحث عن وثائق مطابقة من خلال فهم المعنى السياقي للاستعلام.

### 9. في عملية البحث الدلالي غير المتماثل، عادة ما يكون لديك:

**الإجابة:** استعلام قصير وفقرة أطول تجيب على الاستعلام.

### 10. هل يمكنني استخدام 🤗 Datasets لتحميل البيانات لاستخدامها في مجالات أخرى، مثل معالجة الكلام؟

**الإجابة:** نعم.