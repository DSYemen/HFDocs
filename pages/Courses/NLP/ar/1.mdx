# المقدمة[[introduction]]

مرحبًا بك في دورة Hugging Face! ستوجهك هذه المقدمة خلال إعداد بيئة عمل. إذا كنت قد بدأت للتو في الدورة، نوصيك أولاً بإلقاء نظرة على [الفصل 1](/course/chapter1)، ثم العودة وإعداد بيئتك حتى تتمكن من تجربة الكود بنفسك.

جميع المكتبات التي سنستخدمها في هذه الدورة متاحة كحزم Python، لذلك سنعرض لك هنا كيفية إعداد بيئة Python وتثبيت المكتبات المحددة التي ستحتاجها.

سنغطي طريقتين لإعداد بيئة العمل الخاصة بك، باستخدام دفتر Colab أو بيئة Python افتراضية. لا تتردد في اختيار الطريقة التي تناسبك أكثر. للمبتدئين، نوصي بشدة أن تبدأ باستخدام دفتر Colab.

لاحظ أننا لن نغطي نظام Windows. إذا كنت تستخدم Windows، فننصحك باتباع الخطوات باستخدام دفتر Colab. إذا كنت تستخدم توزيعة Linux أو macOS، فيمكنك استخدام أي من الطريقتين الموضحتين هنا.

تعتمد معظم الدورة على امتلاكك لحساب Hugging Face. نوصي بإنشاء حساب الآن: [إنشاء حساب](https://huggingface.co/join).


## استخدام دفتر Google Colab[[using-a-google-colab-notebook]]

يعد استخدام دفتر Colab أبسط إعداد ممكن؛ قم بتشغيل دفتر في متصفحك وابدأ مباشرةً في البرمجة!

إذا لم تكن معتادًا على Colab، فننصحك بالبدء باتباع [المقدمة](https://colab.research.google.com/notebooks/intro.ipynb). يسمح لك Colab باستخدام بعض الأجهزة المعجلة، مثل وحدات معالجة الرسومات أو وحدات معالجة الموتر، وهو مجاني لأحمال العمل الأصغر.

بمجرد أن تشعر بالراحة في التنقل في Colab، أنشئ دفترًا جديدًا وابدأ في الإعداد:

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter0/new_colab.png" alt="An empty colab notebook" width="80%"/>
</div>

الخطوة التالية هي تثبيت المكتبات التي سنستخدمها في هذه الدورة. سنستخدم `pip` للتثبيت، وهو مدير الحزم لـ Python. في الدفاتر، يمكنك تشغيل أوامر النظام عن طريق وضع حرف `!` قبلها، لذلك يمكنك تثبيت مكتبة 🤗 Transformers كما يلي:

```
!pip install transformers
```

يمكنك التأكد من تثبيت الحزمة بشكل صحيح عن طريق استيرادها داخل وقت تشغيل Python الخاص بك:

```
import transformers
```

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter0/install.gif" alt="A gif showing the result of the two commands above: installation and import" width="80%"/>
</div>

يؤدي هذا إلى تثبيت إصدار خفيف جدًا من 🤗 Transformers. على وجه الخصوص، لا يتم تثبيت أي أطر عمل محددة للتعلم الآلي (مثل PyTorch أو TensorFlow). نظرًا لأننا سنستخدم الكثير من الميزات المختلفة للمكتبة، فإننا نوصي بتثبيت إصدار التطوير، الذي يأتي مع جميع التبعيات المطلوبة لأي حالة استخدام يمكن تخيلها تقريبًا:

```
!pip install transformers[sentencepiece]
```

سيستغرق هذا بعض الوقت، ولكن بعد ذلك ستكون جاهزًا لبقية الدورة!


## استخدام بيئة Python افتراضية[[using-a-python-virtual-environment]]

إذا كنت تفضل استخدام بيئة Python افتراضية، فإن الخطوة الأولى هي تثبيت Python على نظامك. نوصي باتباع [هذا الدليل](https://realpython.com/installing-python/) للبدء.

بمجرد تثبيت Python، يجب أن تكون قادرًا على تشغيل أوامر Python في جهازك الطرفي. يمكنك البدء بتشغيل الأمر التالي للتأكد من تثبيته بشكل صحيح قبل المتابعة إلى الخطوات التالية: `python --version`. يجب أن يطبع هذا إصدار Python المتاح الآن على نظامك.

عند تشغيل أمر Python في جهازك الطرفي، مثل `python --version`، يجب أن تفكر في البرنامج الذي يقوم بتشغيل أمرك على أنه Python "الرئيسي" على نظامك. نوصي بالحفاظ على هذا التثبيت الرئيسي خاليًا من أي حزم، واستخدامه لإنشاء بيئات منفصلة لكل تطبيق تعمل عليه - بهذه الطريقة، يمكن أن يكون لكل تطبيق تبعياته وحزمه الخاصة، ولن تحتاج إلى القلق بشأن مشكلات التوافق المحتملة مع التطبيقات الأخرى.

في Python، يتم ذلك باستخدام [*البيئات الافتراضية*](https://docs.python.org/3/tutorial/venv.html)، وهي أشجار دلائل قائمة بذاتها تحتوي كل منها على تثبيت Python مع إصدار Python معين جنبًا إلى جنب مع جميع الحزم التي يحتاجها التطبيق. يمكن إنشاء مثل هذه البيئة الافتراضية باستخدام عدد من الأدوات المختلفة، لكننا سنستخدم حزمة Python الرسمية لهذا الغرض، والتي تسمى [`venv`](https://docs.python.org/3/library/venv.html#module-venv).

أولاً، أنشئ الدليل الذي تريد أن يكون تطبيقك موجودًا فيه - على سبيل المثال، قد ترغب في إنشاء دليل جديد يسمى *transformers-course* في جذر دليل منزلك:

```
mkdir ~/transformers-course
cd ~/transformers-course
```

من داخل هذا الدليل، أنشئ بيئة افتراضية باستخدام وحدة Python `venv`:

```
python -m venv .env
```

يجب أن يكون لديك الآن دليل يسمى *.env* في مجلدك الفارغ:

```
ls -a
```

```out
.      ..    .env
```

يمكنك الدخول والخروج من بيئتك الافتراضية باستخدام نصوص `activate` و `deactivate`:

```
# تنشيط البيئة الافتراضية
source .env/bin/activate

# إلغاء تنشيط البيئة الافتراضية
deactivate
```

يمكنك التأكد من تنشيط البيئة عن طريق تشغيل الأمر `which python`: إذا كان يشير إلى البيئة الافتراضية، فقد قمت بتنشيطها بنجاح!

```
which python
```

```out
/home/<user>/transformers-course/.env/bin/python
```

### تثبيت التبعيات[[installing-dependencies]]

كما هو الحال في القسم السابق حول استخدام مثيلات Google Colab، ستحتاج الآن إلى تثبيت الحزم المطلوبة للمتابعة. مرة أخرى، يمكنك تثبيت إصدار التطوير من 🤗 Transformers باستخدام مدير الحزم `pip`:

```
pip install "transformers[sentencepiece]"
```

أنت الآن جاهز تمامًا للانطلاق!