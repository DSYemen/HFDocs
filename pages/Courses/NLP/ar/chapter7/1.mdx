<FrameworkSwitchCourse {fw} />

# مقدمة[[introduction]]

<CourseFloatingBanner
    chapter={7}
    classNames="absolute z-10 right-0 top-0"
/>

في [الفصل 3](/course/chapter3)، رأيت كيفية ضبط نموذج لتصنيف النصوص. في هذا الفصل، سنتناول مهام NLP الشائعة التالية:

- تصنيف الرموز
- نمذجة اللغة المقنعة (مثل BERT)
- تلخيص
- ترجمة
- تدريب مسبق على نمذجة اللغة السببية (مثل GPT-2)
- الإجابة على الأسئلة

{#if fw === 'pt'}

للقيام بذلك، ستحتاج إلى الاستفادة من كل ما تعلمته عن واجهة برمجة التطبيقات `Trainer` ومكتبة 🤗 Accelerate في [الفصل 3](/course/chapter3)، ومكتبة 🤗 Datasets في [الفصل 5](/course/chapter5)، ومكتبة 🤗 Tokenizers في [الفصل 6](/course/chapter6). سنقوم أيضًا بتحميل نتائجنا إلى مركز النماذج، كما فعلنا في [الفصل 4](/course/chapter4)، لذا هذا هو الفصل الذي تتجمع فيه كل الأمور!

يمكن قراءة كل قسم بشكل مستقل، وسيوضح لك كيفية تدريب نموذج باستخدام واجهة برمجة التطبيقات `Trainer` أو باستخدام حلقة التدريب الخاصة بك، باستخدام 🤗 Accelerate. لا تتردد في تخطي أي جزء والتركيز على الجزء الذي يهمك أكثر: واجهة برمجة التطبيقات `Trainer` رائعة لضبط أو تدريب نموذجك دون القلق بشأن ما يحدث خلف الكواليس، بينما ستتيح لك حلقة التدريب مع `Accelerate` تخصيص أي جزء تريده بسهولة أكبر.

{:else}

للقيام بذلك، ستحتاج إلى الاستفادة من كل ما تعلمته عن تدريب النماذج باستخدام واجهة برمجة التطبيقات Keras في [الفصل 3](/course/chapter3)، ومكتبة 🤗 Datasets في [الفصل 5](/course/chapter5)، ومكتبة 🤗 Tokenizers في [الفصل 6](/course/chapter6). سنقوم أيضًا بتحميل نتائجنا إلى مركز النماذج، كما فعلنا في [الفصل 4](/course/chapter4)، لذا هذا هو الفصل الذي تتجمع فيه كل الأمور!

يمكن قراءة كل قسم بشكل مستقل.

{/if}


<Tip>

إذا قرأت الأقسام بالترتيب، فستلاحظ أنها تحتوي على الكثير من الرموز والنصوص المشتركة. التكرار مقصود، للسماح لك بالغوص في أي مهمة تهمك (أو العودة لاحقًا) وإيجاد مثال عملي كامل.

</Tip>