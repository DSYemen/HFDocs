<FrameworkSwitchCourse {fw} />

# ضبط نموذج اللغة المقنع [[ضبط-نموذج-اللغة-المقنع]]

{#if fw === 'pt'}

<CourseFloatingBanner chapter={7}
  classNames="absolute z-10 right-0 top-0"
  notebooks={[
    {label: "Google Colab", value: "https://colab.research.google.com/github/huggingface/notebooks/blob/master/course/en/chapter7/section3_pt.ipynb"},
    {label: "Aws Studio", value: "https://studiolab.sagemaker.aws/import/github/huggingface/notebooks/blob/master/course/en/chapter7/section3_pt.ipynb"},
]} />

{:else}

<CourseFloatingBanner chapter={7}
  classNames="absolute z-10 right-0 top-0"
  notebooks={[
    {label: "Google Colab", value: "https://colab.research.google.com/github/huggingface/notebooks/blob/master/course/en/chapter7/section3_tf.ipynb"},
    {label: "Aws Studio", value: "https://studiolab.sagemaker.aws/import/github/huggingface/notebooks/blob/master/course/en/chapter7/section3_tf.ipynb"},
]} />

{/if}

بالنسبة للعديد من تطبيقات NLP التي تتضمن نماذج Transformer، يمكنك ببساطة أخذ نموذج مسبق التدريب من Hugging Face Hub وضبطه مباشرة على بياناتك للمهمة الحالية. بشرط أن لا يكون هناك اختلاف كبير بين مجموعة البيانات المستخدمة في التدريب المسبق ومجموعة البيانات المستخدمة في الضبط، فإن التعلم التحويلي سيؤدي عادة إلى نتائج جيدة.

ومع ذلك، هناك بعض الحالات التي ترغب فيها في ضبط نماذج اللغة على بياناتك أولاً، قبل تدريب رأس محدد للمهمة. على سبيل المثال، إذا كانت مجموعة بياناتك تحتوي على عقود قانونية أو مقالات علمية، فإن نموذج Transformer الأساسي مثل BERT سيعامل عادة الكلمات الخاصة بالمجال في مجموعة بياناتك على أنها رموز نادرة، وقد يكون الأداء الناتج أقل من مرضٍ. من خلال ضبط نموذج اللغة على بيانات المجال، يمكنك تعزيز أداء العديد من المهام اللاحقة، مما يعني أنك عادة ما تحتاج إلى القيام بهذه الخطوة مرة واحدة فقط!

تُسمى عملية ضبط نموذج اللغة مسبق التدريب على بيانات المجال عادةً بـ _تكييف المجال_. وقد اشتهرت في عام 2018 بواسطة [ULMFiT](https://arxiv.org/abs/1801.06146)، والتي كانت واحدة من أولى البنى العصبية (التي تعتمد على LSTM) لجعل التعلم التحويلي يعمل بشكل جيد في NLP. ويُظهر المثال التالي تكييف المجال مع ULMFiT؛ في هذا القسم سنقوم بشيء مشابه، ولكن باستخدام Transformer بدلاً من LSTM!

<div class="flex justify-center">
<img class="block dark:hidden" src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter7/ulmfit.svg" alt="ULMFiT."/>
<img class="hidden dark:block" src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter7/ulmfit-dark.svg" alt="ULMFiT."/>
</div>

بحلول نهاية هذا القسم، سيكون لديك [نموذج لغة مقنع](https://huggingface.co/huggingface-course/distilbert-base-uncased-finetuned-imdb?text=This+is+a+great+%5BMASK%5D.) على Hub يمكنه إكمال الجمل تلقائياً كما هو موضح أدناه:

<iframe src="https://course-demos-distilbert-base-uncased-finetuned-imdb.hf.space" frameBorder="0" height="300" title="Gradio app" class="block dark:hidden container p-0 flex-grow space-iframe" allow="accelerometer; ambient-light-sensor; autoplay; battery; camera; document-domain; encrypted-media; fullscreen; geolocation; gyroscope; layout-animations; legacy-image-formats; magnetometer; microphone; midi; oversized-images; payment; picture-in-picture; publickey-credentials-get; sync-xhr; usb; vr ; wake-lock; xr-spatial-tracking" sandbox="allow-forms allow-modals allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts allow-downloads"></iframe>

لنبدأ!

<Youtube id="mqElG5QJWUg"/>

<Tip>

🙋 إذا كانت مصطلحات "نموذج اللغة المقنع" و"النموذج مسبق التدريب" غير مألوفة لك، اذهب وتحقق من [الفصل 1](/course/chapter1)، حيث نشرح جميع هذه المفاهيم الأساسية، مع مقاطع فيديو!

</Tip>

## اختيار نموذج مسبق التدريب لنموذج اللغة المقنع [[اختيار-نموذج-مسبق-التدريب-لنموذج-اللغة-المقنع]]

لبدء العمل، دعنا نختار نموذجاً مسبق التدريب مناسباً لنموذج اللغة المقنع. كما هو موضح في لقطة الشاشة التالية، يمكنك العثور على قائمة بالمرشحين من خلال تطبيق مرشح "Fill-Mask" على [Hugging Face Hub](https://huggingface.co/models?pipeline_tag=fill-mask&sort=downloads):

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter7/mlm-models.png" alt="Hub models." width="80%"/>
</div>

على الرغم من أن عائلة نماذج BERT وRoBERTa هي الأكثر تنزيلاً، إلا أننا سنستخدم نموذجاً يسمى [DistilBERT](https://huggingface.co/distilbert-base-uncased) 
الذي يمكن تدريبه بشكل أسرع بكثير مع خسارة قليلة أو معدومة في أداء المهام اللاحقة. تم تدريب هذا النموذج باستخدام تقنية خاصة تسمى [_knowledge distillation_](https://en.wikipedia.org/wiki/Knowledge_distillation)، حيث يتم استخدام نموذج "معلم" كبير مثل BERT لتوجيه تدريب نموذج "طالب" له عدد أقل بكثير من المعلمات. إن شرح تفاصيل تقنية knowledge distillation سيأخذنا بعيداً عن هذا القسم، ولكن إذا كنت مهتماً، يمكنك قراءة كل شيء عنها في [_Natural Language Processing with Transformers_](https://www.oreilly.com/library/view/natural-language-processing/9781098136789/) (المعروف باسم كتاب Transformers).

{#if fw === 'pt'}

لننتقل إلى الأمام ونقوم بتنزيل DistilBERT باستخدام فئة `AutoModelForMaskedLM`:

```python
from transformers import AutoModelForMaskedLM

model_checkpoint = "distilbert-base-uncased"
model = AutoModelForMaskedLM.from_pretrained(model_checkpoint)
```

يمكننا معرفة عدد المعلمات التي يمتلكها هذا النموذج من خلال استدعاء طريقة `num_parameters()`:

```python
distilbert_num_parameters = model.num_parameters() / 1_000_000
print(f"'>>> DistilBERT number of parameters: {round(distilbert_num_parameters)}M'")
print(f"'>>> BERT number of parameters: 110M'")
```

```python out
'>>> DistilBERT number of parameters: 67M'
'>>> BERT number of parameters: 110M'
```

{:else}

لننتقل إلى الأمام ونقوم بتنزيل DistilBERT باستخدام فئة `TFAutoModelForMaskedLM`:

```python
from transformers import TFAutoModelForMaskedLM

model_checkpoint = "distilbert-base-uncased"
model = TFAutoModelForMaskedLM.from_pretrained(model_checkpoint)
```

يمكننا معرفة عدد المعلمات التي يمتلكها هذا النموذج من خلال استدعاء طريقة `summary()`:

```python
model.summary()
```

```python out
Model: "tf_distil_bert_for_masked_lm"
_________________________________________________________________
Layer (type)                 Output Shape              Param #   
=================================================================
distilbert (TFDistilBertMain multiple                  66362880  
_________________________________________________________________
vocab_transform (Dense)      multiple                  590592    
_________________________________________________________________
vocab_layer_norm (LayerNorma multiple                  1536      
_________________________________________________________________
vocab_projector (TFDistilBer multiple                  23866170  
=================================================================
Total params: 66,985,530
Trainable params: 66,985,530
Non-trainable params: 0
_________________________________________________________________
```

{/if}

مع حوالي 67 مليون معلمة، فإن DistilBERT أصغر بحوالي مرتين من نموذج BERT الأساسي، مما يترجم إلى تسريع في التدريب بمقدار الضعف - رائع! دعنا الآن نرى أنواع الرموز التي يتوقعها هذا النموذج على أنها الأكثر احتمالاً لإكمال عينة صغيرة من النص:

```python
text = "This is a great [MASK]."
```
كبشر، يمكننا تخيل العديد من الاحتمالات لرموز `[MASK]`، مثل "يوم" أو "رحلة" أو "لوحة". بالنسبة للنماذج المُدربة مسبقًا، تعتمد التوقعات على مجموعة البيانات التي تم تدريب النموذج عليها، حيث يتعلم التقاط الأنماط الإحصائية الموجودة في البيانات. مثل BERT، تم تدريب DistilBERT مسبقًا على مجموعة بيانات [ويكيبيديا الإنجليزية](https://huggingface.co/datasets/wikipedia) و [BookCorpus](https://huggingface.co/datasets/bookcorpus)، لذلك نتوقع أن تعكس التوقعات لـ `[MASK]` هذه المجالات. للتنبؤ بالرموز، نحتاج إلى محزز DistilBERT لإنتاج المدخلات للنموذج، لذا دعنا نقوم بتنزيل ذلك من المركز أيضًا:

```python
from transformers import AutoTokenizer

tokenizer = AutoTokenizer.from_pretrained(model_checkpoint)
```

مع وجود محزز ونموذج، يمكننا الآن تمرير مثال النص الخاص بنا إلى النموذج، واستخراج اللوغاريتمات، وطباعة أفضل 5 مرشحين:

{#if fw === 'pt'}

```python
import torch

inputs = tokenizer(text, return_tensors="pt")
token_logits = model(**inputs).logits
# تحديد موقع [MASK] واستخراج اللوغاريتمات الخاصة به
mask_token_index = torch.where(inputs["input_ids"] == tokenizer.mask_token_id)[1]
mask_token_logits = token_logits[0, mask_token_index, :]
# اختيار مرشحي [MASK] ذوي اللوغاريتمات الأعلى
top_5_tokens = torch.topk(mask_token_logits, 5, dim=1).indices[0].tolist()

for token in top_5_tokens:
    print(f"'>>> {text.replace(tokenizer.mask_token, tokenizer.decode([token]))}'")
```

{:else}

```python
import numpy as np
import tensorflow as tf

inputs = tokenizer(text, return_tensors="np")
token_logits = model(**inputs).logits
# تحديد موقع [MASK] واستخراج اللوغاريتمات الخاصة به
mask_token_index = np.argwhere(inputs["input_ids"] == tokenizer.mask_token_id)[0, 1]
mask_token_logits = token_logits[0, mask_token_index, :]
# اختيار مرشحي [MASK] ذوي اللوغاريتمات الأعلى
# نعكس المصفوفة قبل argsort للحصول على الأكبر، وليس الأصغر، اللوغاريتمات
top_5_tokens = np.argsort(-mask_token_logits)[:5].tolist()

for token in top_5_tokens:
    print(f">>> {text.replace(tokenizer.mask_token, tokenizer.decode([token]))}")
```

{/if}

```python out
'>>> This is a great deal.'
'>>> This is a great success.'
'>>> This is a great adventure.'
'>>> This is a great idea.'
'>>> This is a great feat.'
```

يمكننا أن نرى من المخرجات أن تنبؤات النموذج تشير إلى مصطلحات يومية، وهو أمر ليس مفاجئًا بالنظر إلى أساس ويكيبيديا الإنجليزية. دعنا نرى كيف يمكننا تغيير هذا المجال إلى شيء أكثر تخصصًا - مراجعات الأفلام القطبية للغاية!

## مجموعة البيانات [[the-dataset]]

لعرض تكيف المجال، سنستخدم مجموعة بيانات [Large Movie Review Dataset](https://huggingface.co/datasets/imdb) الشهيرة (أو IMDb اختصارًا)، وهي مجموعة من مراجعات الأفلام التي يتم استخدامها غالبًا لاختبار نماذج تحليل المشاعر. من خلال ضبط DistilBERT الدقيق على هذه المجموعة، نتوقع أن يقوم النموذج اللغوي بتكييف مفرداته من البيانات الواقعية لويكيبيديا التي تم تدريبه عليها مسبقًا إلى العناصر الأكثر ذاتية لمراجعات الأفلام. يمكننا الحصول على البيانات من مركز Hugging Face باستخدام دالة `load_dataset()` من 🤗 Datasets:

```python
from datasets import load_dataset

imdb_dataset = load_dataset("imdb")
imdb_dataset
```

```python out
DatasetDict({
    train: Dataset({
        features: ['text', 'label'],
        num_rows: 25000
    })
    test: Dataset({
        features: ['text', 'label'],
        num_rows: 25000
    })
    unsupervised: Dataset({
        features: ['text', 'label'],
        num_rows: 50000
    })
})
```

يمكننا أن نرى أن قسمي `train` و `test` يتكونان من 25,000 مراجعة لكل منهما، بينما يوجد قسم غير مصنف يسمى `unsupervised` يحتوي على 50,000 مراجعة. دعنا نلقي نظرة على بعض العينات للحصول على فكرة عن نوع النص الذي نتعامل معه. كما فعلنا في الفصول السابقة من الدورة، سنقوم بتوصيل دالتين `Dataset.shuffle()` و `Dataset.select()` لإنشاء عينة عشوائية:

```python
sample = imdb_dataset["train"].shuffle(seed=42).select(range(3))

for row in sample:
    print(f"\n'>>> Review: {row['text']}'")
    print(f"'>>> Label: {row['label']}'")
```

```python out

'>>> Review: هذا هو فيلم Priyadarshan النموذجي - مجموعة من الشخصيات المجنونة في مهمة سخيفة. ذروة توقيعه هي اجتماع جميع شخصيات الفيلم والقتال مع بعضهم البعض في بعض الجنون حول المال المخفي. سواء كان تذكرة يانصيب رابحة في Malamaal Weekly، أو أموال سوداء في Hera Pheri، أو "kodokoo" في Phir Hera Pheri، إلخ، إلخ، أصبح المخرج متوقعًا بشكل سخيف. لا تفهموني خطأ؛ على الرغم من أن أفلامه قد تكون مبتذلة وسخيفة، إلا أنني عادة ما أستمتع بالكوميديا. ومع ذلك، في معظم أفلامه السابقة، كان هناك بعض الفكاهة الجيدة (Hungama و Hera Pheri هي أفلام جديرة بالملاحظة). الآن، تتلاشى طرافة أفلامه حيث يستخدم نفس الصيغة مرارًا وتكرارًا.<br /><br />الأغاني جيدة. تبدو Tanushree Datta رائعة. Rajpal Yadav مزعج، وTusshar ليس أفضل بكثير. Kunal Khemu جيد، وSharman Joshi هو الأفضل.'
'>>> Label: 0'

'>>> Review: حسنًا، القصة لا معنى لها، والشخصيات تفتقر إلى أي بعد، وأفضل حوار هو ارتجالات حول جودة الفيلم السيئة، والتصوير السينمائي بائس، والتحرير فقط ينقذ بعض الفوضى، لكن سام" بيكينباه أخرج الفيلم. بطريقة ما، إخراجه ليس كافيًا. بالنسبة لأولئك الذين يقدرون بيكينباه وأعماله العظيمة، فإن هذا الفيلم مخيب للآمال. حتى وجود طاقم رائع لا يمكنه استرداد الوقت الذي يضيعه المشاهد في هذا الجهد الضئيل.<br /><br />الاستجابة الصحيحة للفيلم هي الاحتقار الذي يجلبه المخرج سان بيكينباه، وجيمس كان، وروبرت دوفال، وبيرت يونغ، وبو هوبكنز، وآرثر هيل، وحتى جيج يونغ إلى عملهم. شاهد أفلام بيكينباه العظيمة. تجنب هذه الفوضى.'
'>>> Label: 0'

'>>> Review: شاهدت هذا الفيلم في السينما عندما كنت في السادسة أو السابعة من عمري. لقد أحببته آنذاك، والآن لدي نسخة VHS. <br /><br />أطفالي الذين تتراوح أعمارهم بين 4 و6 سنوات يحبون هذا الفيلم ويطلبون مشاهدته مرارًا وتكرارًا. <br /><br />لقد استمتعت بمشاهدته مرة أخرى أيضًا. على الرغم من أنه يجب أن أعترف أنه ليس جيدًا على شاشة التلفزيون الصغيرة.<br /><br />ليس لدي أطفال أكبر سنًا لذا لا أعرف ما الذي سيفكرون فيه. <br /><br />الأغاني لطيفة جدًا. ابنتي تستمر في غناءها مرارًا وتكرارًا.<br /><br />آمل أن يساعد ذلك.'
'>>> Label: 1'
```

نعم، هذه بالتأكيد مراجعات أفلام، وإذا كنت كبيرًا بما يكفي، فقد تفهم حتى التعليق في المراجعة الأخيرة حول امتلاك نسخة VHS 😜! على الرغم من أننا لن نحتاج إلى التصنيفات للنمذجة اللغوية، إلا أننا يمكن أن نرى بالفعل أن `0` تشير إلى مراجعة سلبية، في حين أن `1` تقابل مراجعة إيجابية.

<Tip>

✏️ **جربها!** قم بإنشاء عينة عشوائية من الانقسام `غير الخاضع للإشراف` وتحقق من أن التصنيفات ليست `0` ولا `1`. وفي أثناء ذلك، يمكنك أيضًا التحقق من أن التصنيفات في الانقسامات `التدريب` و`الاختبار` هي بالفعل `0` أو `1` - هذه هي فحص السلامة المفيد الذي يجب على كل ممارس NLP القيام به في بداية مشروع جديد!

</Tip>

الآن بعد أن ألقينا نظرة سريعة على البيانات، دعنا نتعمق في إعدادها للنمذجة اللغوية المقنعة. كما سنرى، هناك بعض الخطوات الإضافية التي يحتاج المرء إلى اتخاذها مقارنة بمهام تصنيف التسلسل التي رأيناها في [الفصل 3](/course/chapter3). هيا بنا!

## معالجة البيانات مسبقًا [[preprocessing-the-data]]

<Youtube id="8PmhEIXhBvI"/>

بالنسبة للنمذجة اللغوية التلقائية والنمذجة اللغوية المقنعة، تعد خطوة المعالجة المسبقة الشائعة هي دمج جميع الأمثلة ثم تقسيم المجموعة الكاملة إلى أجزاء ذات حجم متساوٍ. هذا يختلف تمامًا عن نهجنا المعتاد، حيث نقوم ببساطة بتقسيم الأمثلة الفردية. لماذا ندمج كل شيء معًا؟ السبب هو أن الأمثلة الفردية قد يتم تقطيعها إذا كانت طويلة جدًا، مما يؤدي إلى فقدان المعلومات التي قد تكون مفيدة لمهمة النمذجة اللغوية!

لذلك للبدء، سنقوم بتقسيم مجموعتنا كما هو معتاد، ولكن _بدون_ تعيين خيار `truncation=True` في مقسمنا. سنحصل أيضًا على معرفات الكلمات إذا كانت متوفرة ((والتي ستكون متوفرة إذا كنا نستخدم مقسمًا سريعًا، كما هو موضح في [الفصل 6](/course/chapter6/3))، حيث سنحتاجها لاحقًا للقيام بتقنيع الكلمة الكاملة. سنغلف هذا في دالة بسيطة، وفي أثناء ذلك سنزيل أعمدة `النص` و`التصنيف` حيث لم نعد بحاجة إليها:

```python
def tokenize_function(examples):
    result = tokenizer(examples["text"])
    if tokenizer.is_fast:
        result["word_ids"] = [result.word_ids(i) for i in range(len(result["input_ids"]))]
    return result


# Use batched=True to activate fast multithreading!
tokenized_datasets = imdb_dataset.map(
    tokenize_function, batched=True, remove_columns=["text", "label"]
)
tokenized_datasets
```

```python out
DatasetDict({
    train: Dataset({
        features: ['attention_mask', 'input_ids', 'word_ids'],
        num_rows: 25000
    })
    test: Dataset({
        features: ['attention_mask', 'input_ids', 'word_ids'],
        num_rows: 25000
    })
    unsupervised: Dataset({
        features: ['attention_mask', 'input_ids', 'word_ids'],
        num_rows: 50000
    })
})
```

نظرًا لأن DistilBERT هو نموذج مشابه لـ BERT، يمكننا أن نرى أن النصوص المشفرة تتكون من `input_ids` و`attention_mask` التي رأيناها في فصول أخرى، بالإضافة إلى `word_ids` التي أضفناها.

الآن بعد أن قمنا بتقسيم مراجعات أفلامنا، فإن الخطوة التالية هي تجميعها جميعًا معًا وتقسيم النتيجة إلى أجزاء. ولكن كم يجب أن يكون حجم هذه الأجزاء؟ سيتحدد ذلك في النهاية بكمية ذاكرة GPU المتوفرة لديك، ولكن نقطة بداية جيدة هي معرفة ما هو حجم السياق الأقصى للنموذج. يمكن استنتاج ذلك عن طريق فحص سمة `model_max_length` لمقسم الكلمات:

```python
tokenizer.model_max_length
```

```python out
512
```

تُشتق هذه القيمة من ملف *tokenizer_config.json* المرتبط بنقطة تفتيش؛ في هذه الحالة يمكننا أن نرى أن حجم السياق هو 512 رمزًا، تمامًا مثل BERT.

<Tip>

✏️ **جربها!** بعض نماذج المحول، مثل [BigBird](https://huggingface.co/google/bigbird-roberta-base) و[Longformer](hf.co/allenai/longformer-base-4096)، لديها طول سياق أطول بكثير من BERT ونماذج المحول المبكرة الأخرى. قم بتنفيذ مقسم الكلمات لأحد هذه نقاط التفتيش وتحقق من أن `model_max_length` يتفق مع ما هو مذكور في بطاقة النموذج.

</Tip>

لذلك، من أجل تشغيل تجاربنا على وحدات معالجة الرسومات مثل تلك الموجودة على Google Colab، سنختار شيئًا أصغر قليلاً يمكن أن يناسب الذاكرة:

```python
chunk_size = 128
```

<Tip warning={true}>

لاحظ أن استخدام حجم جزء صغير يمكن أن يكون ضارًا في السيناريوهات الواقعية، لذا يجب عليك استخدام حجم يتوافق مع حالة الاستخدام التي ستطبق نموذجك عليها.

</Tip>

الآن يأتي الجزء الممتع. لإظهار كيفية عمل الدمج، دعنا نأخذ بعض المراجعات من مجموعة التدريب المقسمة لدينا ونطبع عدد الرموز لكل مراجعة:

```python
# Slicing produces a list of lists for each feature
tokenized_samples = tokenized_datasets["train"][:3]

for idx, sample in enumerate(tokenized_samples["input_ids"]):
    print(f"'>>> Review {idx} length: {len(sample)}'")
```

```python out
'>>> Review 0 length: 200'
'>>> Review 1 length: 559'
'>>> Review 2 length: 192'
```

بعد ذلك يمكننا دمج جميع هذه الأمثلة باستخدام تعبير معجمي بسيط، كما يلي:

```python
concatenated_examples = {
    k: sum(tokenized_samples[k], []) for k in tokenized_samples.keys()
}
total_length = len(concatenated_examples["input_ids"])
print(f"'>>> Concatenated reviews length: {total_length}'")
```

```python out
'>>> Concatenated reviews length: 951'
```

رائع، يتحقق الطول الإجمالي - لذا دعنا نقسم المراجعات المدمجة إلى أجزاء بالحجم المعطى بواسطة `chunk_size`. للقيام بذلك، نكرر عبر الميزات في `concatenated_examples` ونستخدم تعبيرًا معجميًا لإنشاء شرائح لكل ميزة. النتيجة هي معجم من الأجزاء لكل ميزة:

```python
chunks = {
    k: [t[i : i + chunk_size] for i in range(0, total_length, chunk_size)]
    for k, t in concatenated_examples.items()
}

for chunk in chunks["input_ids"]:
    print(f"'>>> Chunk length: {len(chunk)}'")
```

```python out
'>>> Chunk length: 128'
'>>> Chunk length: 128'
'>>> Chunk length: 128'
'>>> Chunk length: 128'
'>>> Chunk length: 128'
'>>> Chunk length: 128'
'>>> Chunk length: 128'
'>>> Chunk length: 55'
```

كما يمكنك أن ترى في هذا المثال، سيكون الجزء الأخير أصغر بشكل عام من حجم الجزء الأقصى. هناك استراتيجيتان رئيسيتان للتعامل مع هذا:

* تجاهل الجزء الأخير إذا كان أصغر من `chunk_size`.
* إضافة حشو إلى الجزء الأخير حتى يساوي طوله `chunk_size`.

سنأخذ النهج الأول هنا، لذا دعنا نغلف كل المنطق أعلاه في دالة واحدة يمكننا تطبيقها على مجموعاتنا المقسمة:

```python
def group_texts(examples):
    # Concatenate all texts
    concatenated_examples = {k: sum(examples[k], []) for k in examples.keys()}
    # Compute length of concatenated texts
    total_length = len(concatenated_examples[list(examples.keys())[0]])
    # We drop the last chunk if it's smaller than chunk_size
    total_length = (total_length // chunk_size) * chunk_size
    # Split by chunks of max_len
    result = {
        k: [t[i : i + chunk_size] for i in range(0, total_length, chunk_size)]
        for k, t in concatenated_examples.items()
    }
    # Create a new labels column
    result["labels"] = result["input_ids"].copy()
    return result
```

لاحظ أنه في الخطوة الأخيرة من `group_texts()` ننشئ عمود `labels` جديدًا وهو نسخة من عمود `input_ids`. كما سنرى بعد قليل، ذلك لأن الهدف في النمذجة اللغوية المقنعة هو التنبؤ بالرموز المقنعة عشوائيًا في دفعة الإدخال، ومن خلال إنشاء عمود `labels` نوفر الحقيقة الأساسية لنموذج اللغة الخاص بنا ليتعلم منها.

دعنا الآن نطبق `group_texts()` على مجموعاتنا المقسمة باستخدام دالة `Dataset.map()` الموثوقة لدينا:

```python
lm_datasets = tokenized_datasets.map(group_texts, batched=True)
lm_datasets
```

```python out
DatasetDict({
    train: Dataset({
        features: ['attention_mask', 'input_ids', 'labels', 'word_ids'],
        num_rows: 61289
    })
    test: Dataset({
        features: ['attention_mask', 'input_ids', 'labels', 'word_ids'],
        num_rows: 59905
    })
    unsupervised: Dataset({
        features: ['attention_mask', 'input_ids', 'labels', 'word_ids'],
        num_rows: 122963
    })
})
يمكنك أن ترى أن تجميع النصوص ثم تقسيمها قد أنتج العديد من الأمثلة أكثر من 25,000 الأصلية في مجموعات "train" و "test". وذلك لأننا الآن لدينا أمثلة تتضمن _رموز متجاورة_ تمتد عبر أمثلة متعددة من النص الأصلي. يمكنك أن ترى ذلك بوضوح من خلال البحث عن الرموز الخاصة `[SEP]` و `[CLS]` في إحدى المقاطع:

```python
tokenizer.decode(lm_datasets["train"][1]["input_ids"])
```

```python out
".... at.......... high. a classic line : inspector : i'm here to sack one of your teachers. student : welcome to bromwell high. i expect that many adults of my age think that bromwell high is far fetched. what a pity that it isn't! [SEP] [CLS] homelessness ( or houselessness as george carlin stated ) has been an issue for years but never a plan to help those on the street that were once considered human who did everything from going to school, work, or vote for the matter. most people think of the homeless"
```

في هذا المثال، يمكنك أن ترى مراجعتين متداخلتين للفيلم، واحدة عن فيلم مدرسي والأخرى عن التشرد. دعنا نلقي نظرة أيضًا على شكل التصنيفات في نمذجة اللغة المقنعة:

```python out
tokenizer.decode(lm_datasets["train"][1]["labels"])
```

```python out
".... at.......... high. a classic line : inspector : i'm here to sack one of your teachers. student : welcome to bromwell high. i expect that many adults of my age think that bromwell high is far fetched. what a pity that it isn't! [SEP] [CLS] homelessness ( or houselessness as george carlin stated ) has been an issue for years but never a plan to help those on the street that were once considered human who did everything from going to school, work, or vote for the matter. most people think of the homeless"
```

كما هو متوقع من دالة `group_texts()` أعلاه، يبدو هذا مطابقًا لـ `input_ids` بعد فك التشفير - ولكن كيف يمكن لنموذجنا أن يتعلم أي شيء؟ نفتقد خطوة رئيسية: إدراج رموز `[MASK]` في مواضع عشوائية في المدخلات! دعنا نرى كيف يمكننا القيام بذلك أثناء الضبط الدقيق باستخدام جامع بيانات خاص.

## ضبط نموذج DistilBERT باستخدام واجهة برمجة التطبيقات `Trainer`[[fine-tuning-distilbert-with-the-trainer-api]]

ضبط نموذج اللغة المقنعة يشبه إلى حد كبير ضبط نموذج تصنيف التسلسل، كما فعلنا في [الفصل 3](/course/chapter3). الاختلاف الوحيد هو أننا نحتاج إلى جامع بيانات خاص يمكنه إخفاء بعض الرموز بشكل عشوائي في كل دفعة من النصوص. لحسن الحظ، يأتي 🤗 Transformers مزودًا بـ `DataCollatorForLanguageModeling` المخصص لهذه المهمة بالتحديد. كل ما علينا فعله هو تمرير الرموز ووسيط `mlm_probability` الذي يحدد نسبة الرموز التي سيتم إخفاؤها. سنختار 15%، وهي النسبة المستخدمة في نموذج BERT وخيار شائع في الأدبيات:

```python
from transformers import DataCollatorForLanguageModeling

data_collator = DataCollatorForLanguageModeling(tokenizer=tokenizer, mlm_probability=0.15)
```

لمعرفة كيفية عمل الإخفاء العشوائي، دعنا نطعم جامع البيانات ببعض الأمثلة. حيث أنه يتوقع قائمة من `dict`s، حيث يمثل كل `dict` مقطعًا واحدًا من النص المتصل، نقوم أولاً بالتنقل خلال مجموعة البيانات قبل إطعام الدفعة إلى جامع البيانات. نقوم بإزالة مفتاح `"word_ids"` لهذا جامع البيانات حيث أنه لا يتوقع وجوده:

```python
samples = [lm_datasets["train"][i] for i in range(2)]
for sample in samples:
    _ = sample.pop("word_ids")

for chunk in data_collator(samples)["input_ids"]:
    print(f"\n'>>> {tokenizer.decode(chunk)}'")
```

```python output
'>>> [CLS] bromwell [MASK] is a cartoon comedy. it ran at the same [MASK] as some other [MASK] about school life, [MASK] as " teachers ". [MASK] [MASK] [MASK] in the teaching [MASK] lead [MASK] to believe that bromwell high\'[MASK] satire is much closer to reality than is " teachers ". the scramble [MASK] [MASK] financially, the [MASK]ful students whogn [MASK] right through [MASK] pathetic teachers\'pomp, the pettiness of the whole situation, distinction remind me of the schools i knew and their students. when i saw [MASK] episode in [MASK] a student repeatedly tried to burn down the school, [MASK] immediately recalled. [MASK]...'

'>>> .... at.. [MASK]... [MASK]... high. a classic line plucked inspector : i\'[MASK] here to [MASK] one of your [MASK]. student : welcome to bromwell [MASK]. i expect that many adults of my age think that [MASK]mwell [MASK] is [MASK] fetched. what a pity that it isn\'t! [SEP] [CLS] [MASK]ness ( or [MASK]lessness as george 宇in stated )公 been an issue for years but never [MASK] plan to help those on the street that were once considered human [MASK] did everything from going to school, [MASK], [MASK] vote for the matter. most people think [MASK] the homeless'
```

رائع، لقد نجح الأمر! يمكننا أن نرى أن الرمز `[MASK]` قد تم إدراجه بشكل عشوائي في مواقع مختلفة في نصنا. ستكون هذه الرموز هي التي سيتعين على نموذجنا التنبؤ بها أثناء التدريب - وجمال جامع البيانات يكمن في أنه سيقوم بعشوائية إدراج الرمز `[MASK]` مع كل دفعة!

<Tip>

✏️ **جرب الأمر بنفسك!** قم بتشغيل مقطع الكود أعلاه عدة مرات لترى عملية الإخفاء العشوائي تحدث أمام عينيك! قم أيضًا باستبدال دالة `tokenizer.decode()` بدالة `tokenizer.convert_ids_to_tokens()` لترى أنه في بعض الأحيان يتم إخفاء رمز واحد من كلمة معينة، وليس الرموز الأخرى.

</Tip>

{#if fw === 'pt'}

أحد الآثار الجانبية للإخفاء العشوائي هو أن مقاييس التقييم لدينا لن تكون حتمية عند استخدام `Trainer`، حيث أننا نستخدم نفس جامع البيانات لمجموعات التدريب والاختبار. سنرى لاحقًا، عند النظر في الضبط الدقيق باستخدام 🤗 Accelerate، كيف يمكننا استخدام مرونة حلقة التقييم المخصصة لتجميد العشوائية.

{/if}

عند تدريب النماذج لنمذجة اللغة المقنعة، يمكن استخدام تقنية إخفاء الكلمات الكاملة معًا، وليس فقط الرموز الفردية. يُطلق على هذا النهج اسم _إخفاء الكلمة الكاملة_. إذا أردنا استخدام إخفاء الكلمة الكاملة، فسنحتاج إلى بناء جامع بيانات بأنفسنا. جامع البيانات هو مجرد دالة تأخذ قائمة من العينات وتحولها إلى دفعة، لذا دعنا نفعل ذلك الآن! سنستخدم معرّفات الكلمات المحسوبة سابقًا لإنشاء خريطة بين مؤشرات الكلمات والرموز المقابلة، ثم نقرر عشوائيًا الكلمات التي سيتم إخفاؤها وتطبيق الإخفاء على المدخلات. لاحظ أن التصنيفات جميعها `-100` باستثناء تلك التي تقابل كلمات الإخفاء.

{#if fw === 'pt'}

```py
import collections
import numpy as np

from transformers import default_data_collator

wwm_probability = 0.2


def whole_word_masking_data_collator(features):
    for feature in features:
        word_ids = feature.pop("word_ids")

        # Create a map between words and corresponding token indices
        mapping = collections.defaultdict(list)
        current_word_index = -1
        current_word = None
        for idx, word_id in enumerate(word_ids):
            if word_id is not None:
                if word_id != current_word:
                    current_word = word_id
                    current_word_index += 1
                mapping[current_word_index].append(idx)

        # Randomly mask words
        mask = np.random.binomial(1, wwm_probability, (len(mapping),))
        input_ids = feature["input_ids"]
        labels = feature["labels"]
        new_labels = [-100] * len(labels)
        for word_id in np.where(mask)[0]:
            word_id = word_id.item()
            for idx in mapping[word_id]:
                new_labels[idx] = labels[idx]
                input_ids[idx] = tokenizer.mask_token_id
        feature["labels"] = new_labels

    return default_data_collator(features)
```
{:else}

```py
import collections
import numpy as np

from transformers.data.data_collator import tf_default_data_collator

wwm_probability = 0.2


def whole_word_masking_data_collator(features):
    for feature in features:
        word_ids = feature.pop("word_ids")

        # إنشاء خريطة بين الكلمات ومؤشرات الرموز المقابلة
        mapping = collections.defaultdict(list)
        current_word_index = -1
        current_word = None
        for idx, word_id in enumerate(word_ids):
            if word_id is not None:
                if word_id != current_word:
                    current_word = word_id
                    current_word_index += 1
                mapping[current_word_index].append(idx)

        # إخفاء الكلمات بشكل عشوائي
        mask = np.random.binomial(1, wwm_probability, (len(mapping),))
        input_ids = feature["input_ids"]
        labels = feature["labels"]
        new_labels = [-100] * len(labels)
        for word_id in np.where(mask)[0]:
            word_id = word_id.item()
            for idx in mapping[word_id]:
                new_labels[idx] = labels[idx]
                input_ids[idx] = tokenizer.mask_token_id
        feature["labels"] = new_labels

    return tf_default_data_collator(features)
```

{/if}

الآن، يمكننا تجربتها على نفس العينات السابقة:

```py
samples = [lm_datasets["train"][i] for i in range(2)]
batch = whole_word_masking_data_collator(samples)

for chunk in batch["input_ids"]:
    print(f"\n'>>> {tokenizer.decode(chunk)}'")
```

```python out
'>>> [CLS] bromwell high is a cartoon comedy [MASK] it ran at the same time as some other programs about school life, such as " teachers ". my 35 years in the teaching profession lead me to believe that bromwell high\'s satire is much closer to reality than is " teachers ". the scramble to survive financially, the insightful students who can see right through their pathetic teachers\'pomp, the pettiness of the whole situation, all remind me of the schools i knew and their students. when i saw the episode in which a student repeatedly tried to burn down the school, i immediately recalled.....'

'>>> .... [MASK] [MASK] [MASK] [MASK]....... high. a classic line : inspector : i\'m here to sack one of your teachers. student : welcome to bromwell high. i expect that many adults of my age think that bromwell high is far fetched. what a pity that it isn\'t! [SEP] [CLS] homelessness ( or houselessness as george carlin stated ) has been an issue for years but never a plan to help those on the street that were once considered human who did everything from going to school, work, or vote for the matter. most people think of the homeless'
```

<Tip>

✏️ **جربها بنفسك!** قم بتشغيل مقطع الكود أعلاه عدة مرات لترى عملية الإخفاء العشوائي تحدث أمام عينيك! قم أيضاً باستبدال طريقة `tokenizer.decode()` بالطريقة `tokenizer.convert_ids_to_tokens()` لترى أن الرموز من كلمة معينة يتم إخفاؤها معاً.

</Tip>

الآن بعد أن أصبح لدينا أداتان لتجميع البيانات، فإن بقية خطوات الضبط الدقيق (fine-tuning) هي خطوات قياسية. قد يستغرق التدريب بعض الوقت على Google Colab إذا لم تكن محظوظاً بما يكفي للحصول على وحدة معالجة الرسوميات (GPU) الأسطورية P100 😭، لذلك سنقوم أولاً بتخفيض حجم مجموعة التدريب إلى بضعة آلاف من الأمثلة. لا تقلق، سنحصل على نموذج لغة جيد جداً! هناك طريقة سريعة لتخفيض حجم مجموعة بيانات في 🤗 Datasets وهي عبر وظيفة `Dataset.train_test_split()` التي رأيناها في [الفصل 5](/course/chapter5):

```python
train_size = 10_000
test_size = int(0.1 * train_size)

downsampled_dataset = lm_datasets["train"].train_test_split(
    train_size=train_size, test_size=test_size, seed=42
)
downsampled_dataset
```

```python out
DatasetDict({
    train: Dataset({
        features: ['attention_mask', 'input_ids', 'labels', 'word_ids'],
        num_rows: 10000
    })
    test: Dataset({
        features: ['attention_mask', 'input_ids', 'labels', 'word_ids'],
        num_rows: 1000
    })
})
```

هذا قام تلقائياً بإنشاء أقسام جديدة للتدريب والاختبار، مع تحديد حجم مجموعة التدريب إلى 10,000 مثال وحجم مجموعة التحقق إلى 10% من ذلك -- يمكنك زيادة هذا الحجم إذا كان لديك وحدة معالجة رسوميات قوية! الشيء التالي الذي نحتاج إلى فعله هو تسجيل الدخول إلى Hugging Face Hub. إذا كنت تقوم بتشغيل هذا الكود في دفتر ملاحظات، يمكنك فعل ذلك باستخدام وظيفة المساعدة التالية:

```python
from huggingface_hub import notebook_login

notebook_login()
```

والتي ستعرض أداة يمكنك من خلالها إدخال بيانات الاعتماد الخاصة بك. بدلاً من ذلك، يمكنك تشغيل:

```
huggingface-cli login
```

في المحطة الطرفية المفضلة لديك وتسجيل الدخول هناك.

{#if fw === 'tf'}

بمجرد تسجيل الدخول، يمكننا إنشاء مجموعات بيانات `tf.data`. للقيام بذلك، سنستخدم طريقة `prepare_tf_dataset()`، والتي تستخدم نموذجنا لاستنتاج الأعمدة التي يجب إدخالها في مجموعة البيانات تلقائياً. إذا كنت تريد التحكم في الأعمدة التي سيتم استخدامها بالضبط، يمكنك استخدام طريقة `Dataset.to_tf_dataset()` بدلاً من ذلك. للحفاظ على البساطة، سنستخدم أداة تجميع البيانات القياسية هنا، ولكن يمكنك أيضاً تجربة أداة تجميع البيانات لإخفاء الكلمات بالكامل ومقارنة النتائج كتدريب:

```python
tf_train_dataset = model.prepare_tf_dataset(
    downsampled_dataset["train"],
    collate_fn=data_collator,
    shuffle=True,
    batch_size=32,
)

tf_eval_dataset = model.prepare_tf_dataset(
    downsampled_dataset["test"],
    collate_fn=data_collator,
    shuffle=False,
    batch_size=32,
)
```

بعد ذلك، نقوم بإعداد معاملات التدريب ونقوم بتجميع نموذجنا. نستخدم وظيفة `create_optimizer()` من مكتبة 🤗 Transformers، والتي تعطينا محسن `AdamW` مع انخفاض معدل التعلم الخطي. نستخدم أيضاً الخسارة المدمجة للنموذج، والتي تكون الافتراضية عندما لا يتم تحديد خسارة كحجة لـ `compile()`، ونقوم بضبط دقة التدريب إلى `"mixed_float16"`. لاحظ أنه إذا كنت تستخدم وحدة معالجة رسوميات Colab أو وحدة معالجة رسوميات أخرى لا تدعم float16 المعجل، فيجب عليك على الأرجح تعليق ذلك السطر.

بالإضافة إلى ذلك، نقوم بإعداد `PushToHubCallback` التي ستقوم بحفظ النموذج في Hub بعد كل حقبة. يمكنك تحديد اسم المستودع الذي تريد دفعه باستخدام حجة `hub_model_id` (على وجه الخصوص، سيتعين عليك استخدام هذه الحجة للدفع إلى منظمة). على سبيل المثال، لدفع النموذج إلى منظمة [`huggingface-course`](https://huggingface.co/huggingface-course)، أضفنا `hub_model_id="huggingface-course/distilbert-finetuned-imdb"`. بشكل افتراضي، سيتم استخدام المستودع الموجود في مساحة الاسم الخاصة بك والمسمى باسم دليل الإخراج الذي قمت بتعيينه، لذلك في حالتنا سيكون `"lewtun/distilbert-finetuned-imdb"`.

```python
from transformers import create_optimizer
from transformers.keras_callbacks import PushToHubCallback
import tensorflow as tf

num_train_steps = len(tf_train_dataset)
optimizer, schedule = create_optimizer(
    init_lr=2e-5,
    num_warmup_steps=1_000,
    num_train_steps=num_train_steps,
    weight_decay_rate=0.01,
)
model.compile(optimizer=optimizer)

# Train in mixed-precision float16
tf.keras.mixed_precision.set_global_policy("mixed_float16")

model_name = model_checkpoint.split("/")[-1]
callback = PushToHubCallback(
    output_dir=f"{model_name}-finetuned-imdb", tokenizer=tokenizer
)
```

الآن نحن مستعدون لتشغيل `model.fit()` -- ولكن قبل القيام بذلك دعونا نلقي نظرة سريعة على _perplexity_، والتي هي مقياس شائع لتقييم أداء نماذج اللغة.

{:else}

بمجرد تسجيل الدخول، يمكننا تحديد الحجج لـ `Trainer`:

```python
from transformers import TrainingArguments

batch_size = 64
# عرض خسارة التدريب مع كل حقبة
logging_steps = len(downsampled_dataset["train"]) // batch_size
model_name = model_checkpoint.split("/")[-1]

training_args = TrainingArguments(
    output_dir=f"{model_name}-finetuned-imdb",
    overwrite_output_dir=True,
    evaluation_strategy="epoch",
    learning_rate=2e-5,
    weight_decay=0.01,
    per_device_train_batch_size=batch_size,
    per_device_eval_batch_size=batch_size,
    push_to_hub=True,
    fp16=True,
    logging_steps=logging_steps,
)
```

هنا قمنا بتعديل بعض الخيارات الافتراضية، بما في ذلك `logging_steps` لضمان تتبعنا لخسارة التدريب مع كل دورة. كما استخدمنا `fp16=True` لتمكين التدريب عالي الدقة، مما يعطينا دفعة أخرى في السرعة. بشكل افتراضي، سيقوم `Trainer` بإزالة أي أعمدة ليست جزءًا من طريقة `forward()` للنموذج. هذا يعني أنه إذا كنت تستخدم جامع البيانات لحجب الكلمات الكاملة، فستحتاج أيضًا إلى تعيين `remove_unused_columns=False` لضمان عدم فقدان عمود `word_ids` أثناء التدريب.

لاحظ أنه يمكنك تحديد اسم المستودع الذي تريد الدفع إليه باستخدام حجة `hub_model_id` (على وجه الخصوص، سيتعين عليك استخدام هذه الحجة للدفع إلى منظمة). على سبيل المثال، عندما قمنا بدفع النموذج إلى منظمة [`huggingface-course`](https://huggingface.co/huggingface-course)، أضفنا `hub_model_id="huggingface-course/distilbert-finetuned-imdb"` إلى `TrainingArguments`. بشكل افتراضي، سيتم استخدام المستودع في مساحة الاسم الخاصة بك وسيتم تسميته وفقًا لدليل الإخراج الذي قمت بتعيينه، لذا في حالتنا سيكون `"lewtun/distilbert-finetuned-imdb"`.

الآن لدينا جميع المكونات لتهيئة `Trainer`. هنا نستخدم جامع البيانات القياسي، ولكن يمكنك تجربة جامع بيانات حجب الكلمات الكاملة ومقارنة النتائج كتدريب:

```python
from transformers import Trainer

trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=downsampled_dataset["train"],
    eval_dataset=downsampled_dataset["test"],
    data_collator=data_collator,
    tokenizer=tokenizer,
)
```

نحن الآن مستعدون لتشغيل `trainer.train()` -- ولكن قبل القيام بذلك، دعنا نلقي نظرة سريعة على _perplexity_، وهي مقياس شائع لتقييم أداء نماذج اللغة.

{/if}

### Perplexity لنماذج اللغة[[perplexity-for-language-models]]

<Youtube id="NURcDHhYe98"/>

على عكس المهام الأخرى مثل تصنيف النصوص أو الإجابة على الأسئلة حيث يتم منحنا مجموعة بيانات موسومة للتدريب، في نمذجة اللغة لا نمتلك أي تسميات صريحة. إذن، كيف نحدد ما يجعل نموذج اللغة جيدًا؟ كما هو الحال مع ميزة التصحيح التلقائي في هاتفك، فإن نموذج اللغة الجيد هو الذي يعطي احتمالات عالية للجمل التي تكون صحيحة نحوياً، واحتمالات منخفضة للجمل غير المفهومة. لإعطائك فكرة أفضل عن الشكل الذي يبدو عليه هذا، يمكنك العثور على مجموعات كاملة من "فشل التصحيح التلقائي" عبر الإنترنت، حيث ينتج النموذج في هاتف الشخص بعض الإكمال المضحك (وغير المناسب في كثير من الأحيان)!

{#if fw === 'pt'}

بافتراض أن مجموعة الاختبار لدينا تتكون بشكل أساسي من جمل صحيحة نحوياً، فإن إحدى طرق قياس جودة نموذج اللغة لدينا هي حساب الاحتمالات التي يعينها للجملة التالية في جميع جمل مجموعة الاختبار. تشير الاحتمالات العالية إلى أن النموذج ليس "مندهشًا" أو "محتارًا" بالأمثلة غير المرئية، ويشير إلى أنه قد تعلم الأنماط الأساسية للنحو في اللغة. هناك تعريفات رياضية مختلفة للـ perplexity، ولكن التعريف الذي سنستخدمه يعرّفه بأنه الأسّي للخسارة المتقاطعة. وبالتالي، يمكننا حساب الـ perplexity للنموذج المسبق التدريب باستخدام دالة `Trainer.evaluate()` لحساب الخسارة المتقاطعة على مجموعة الاختبار ثم أخذ الأسّي للنتيجة:

```python
import math

eval_results = trainer.evaluate()
print(f">>> Perplexity: {math.exp(eval_results['eval_loss']):.2f}")
```

{:else}

بافتراض أن مجموعة الاختبار لدينا تتكون بشكل أساسي من جمل صحيحة نحوياً، فإن إحدى طرق قياس جودة نموذج اللغة لدينا هي حساب الاحتمالات التي يعينها للجملة التالية في جميع جمل مجموعة الاختبار. تشير الاحتمالات العالية إلى أن النموذج يشير إلى أن النموذج ليس "مندهشًا" أو "محتارًا" بالأمثلة غير المرئية، ويشير إلى أنه قد تعلم الأنماط الأساسية للنحو في اللغة. هناك تعريفات رياضية مختلفة للـ perplexity، ولكن التعريف الذي سنستخدمه يعرّفه بأنه الأسّي للخسارة المتقاطعة. وبالتالي، يمكننا حساب الـ perplexity للنموذج المسبق التدريب باستخدام طريقة `model.evaluate()` لحساب الخسارة المتقاطعة على مجموعة الاختبار ثم أخذ الأسّي للنتيجة:

```python
import math

eval_loss = model.evaluate(tf_eval_dataset)
print(f"Perplexity: {math.exp(eval_loss):.2f}")
```

{/if}

```python out
>>> Perplexity: 21.75
```

يشير انخفاض درجة الـ perplexity إلى نموذج لغة أفضل، ويمكننا هنا أن نرى أن نموذجنا الأولي له قيمة كبيرة إلى حد ما. دعنا نرى إن كان بإمكاننا خفضها من خلال الضبط الدقيق! للقيام بذلك، سنقوم بتشغيل حلقة التدريب أولاً:

{#if fw === 'pt'}

```python
trainer.train()
```

{:else}

```python
model.fit(tf_train_dataset, validation_data=tf_eval_dataset, callbacks=[callback])
```

{/if}

ثم حساب الـ perplexity الناتج على مجموعة الاختبار كما فعلنا سابقًا:

{#if fw === 'pt'}

```python
eval_results = trainer.evaluate()
print(f">>> Perplexity: {math.exp(eval_results['eval_loss']):.2f}")
```

{:else}

```python
eval_loss = model.evaluate(tf_eval_dataset)
print(f"Perplexity: {math.exp(eval_loss):.2f}")
```

{/if}

```python out
>>> Perplexity: 11.32
```

رائع -- هذا انخفاض كبير في الـ perplexity، مما يخبرنا أن النموذج قد تعلم شيئًا عن مجال مراجعات الأفلام!

{#if fw === 'pt'}

بمجرد انتهاء التدريب، يمكننا دفع بطاقة النموذج مع معلومات التدريب إلى المركز (يتم حفظ نقاط التفتيش أثناء التدريب نفسه):

```python
trainer.push_to_hub()
```

{/if}

<Tip>

✏️ **دورك!** قم بتشغيل التدريب أعلاه بعد تغيير جامع البيانات إلى جامع بيانات حجب الكلمات الكاملة. هل تحصل على نتائج أفضل؟

</Tip>

{#if fw === 'pt'}

في حالتنا الاستخدامية، لم نكن بحاجة إلى القيام بأي شيء خاص مع حلقة التدريب، ولكن في بعض الحالات قد تحتاج إلى تنفيذ بعض المنطق المخصص. لهذه التطبيقات، يمكنك استخدام 🤗 Accelerate -- دعنا نلقي نظرة!

## الضبط الدقيق لـ DistilBERT مع 🤗 Accelerate[[fine-tuning-distilbert-with-accelerate]]

كما رأينا مع `Trainer`، فإن الضبط الدقيق لنموذج اللغة المقنّع يشبه إلى حد كبير مثال تصنيف النصوص من [الفصل 3](/course/chapter3). في الواقع، الدقة الوحيدة هي استخدام جامع بيانات خاص، وقد غطينا ذلك بالفعل في وقت سابق من هذا القسم!

ولكننا رأينا أن `DataCollatorForLanguageModeling` يطبق أيضًا التعتيم العشوائي مع كل تقييم، لذا سنرى بعض التقلبات في درجات الـ perplexity مع كل تشغيل للتدريب. إحدى طرق القضاء على هذا المصدر من العشوائية هي تطبيق التعتيم _مرة واحدة_ على مجموعة الاختبار بأكملها، ثم استخدام جامع البيانات الافتراضي في 🤗 Transformers لجمع الدفعات أثناء التقييم. لمعرفة كيفية عمل ذلك، دعنا ننفذ دالة بسيطة تطبق التعتيم على دفعة، مشابهة لتجربتنا الأولى مع `DataCollatorForLanguageModeling`:

```python
def insert_random_mask(batch):
    features = [dict(zip(batch, t)) for t in zip(*batch.values())]
    masked_inputs = data_collator(features)
    # إنشاء عمود "معتّم" جديد لكل عمود في مجموعة البيانات
    return {"masked_" + k: v.numpy() for k, v in masked_inputs.items()}
```

بعد ذلك، سنطبق هذه الدالة على مجموعة الاختبار لدينا ونحذف الأعمدة غير المعتّمة حتى نتمكن من استبدالها بالأعمدة المعتّمة. يمكنك استخدام حجب الكلمات الكاملة عن طريق استبدال `data_collator` أعلاه بالمناسب، وفي هذه الحالة يجب عليك إزالة السطر الأول هنا:

```py
downsampled_dataset = downsampled_dataset.remove_columns(["word_ids"])
eval_dataset = downsampled_dataset["test"].map(
    insert_random_mask,
    batched=True,
    remove_columns=downsampled_dataset["test"].column_names,
)
eval_dataset = eval_dataset.rename_columns(
    {
        "masked_input_ids": "input_ids",
        "masked_attention_mask": "attention_mask",
        "masked_labels": "labels",
    }
)
```

بعد ذلك، يمكننا إعداد محمّلات البيانات كالمعتاد، ولكننا سنستخدم `default_data_collator` من 🤗 Transformers لمجموعة البيانات التقييمية:

```python
from torch.utils.data import DataLoader
from transformers import default_data_collator

batch_size = 64
train_dataloader = DataLoader(
    downsampled_dataset["train"],
    shuffle=True,
    batch_size=batch_size,
    collate_fn=data_collator,
)
eval_dataloader = DataLoader(
    eval_dataset, batch_size=batch_size, collate_fn=default_data_collator
)
من هنا، نتبع الخطوات القياسية مع 🤗 Accelerate. أول شيء يجب القيام به هو تحميل نسخة جديدة من النموذج المُدرب مسبقًا:

```
model = AutoModelForMaskedLM.from_pretrained(model_checkpoint)
```

ثم نحتاج إلى تحديد المُحسن؛ سنستخدم `AdamW` القياسي:

```python
from torch.optim import AdamW

optimizer = AdamW(model.parameters(), lr=5e-5)
```

مع هذه الأشياء، يمكننا الآن إعداد كل شيء للتدريب باستخدام كائن `Accelerator`:

```python
from accelerate import Accelerator

accelerator = Accelerator()
model, optimizer, train_dataloader, eval_dataloader = accelerator.prepare(
    model, optimizer, train_dataloader, eval_dataloader
)
```

الآن بعد أن قمنا بتكوين النموذج والمُحسن ومُحملات البيانات، يمكننا تحديد جدول التعلم كما يلي:

```python
from transformers import get_scheduler

num_train_epochs = 3
num_update_steps_per_epoch = len(train_dataloader)
num_training_steps = num_train_epochs * num_update_steps_per_epoch

lr_scheduler = get_scheduler(
    "linear",
    optimizer=optimizer,
    num_warmup_steps=0,
    num_training_steps=num_training_steps,
)
```

هناك شيء واحد فقط يجب القيام به قبل التدريب: إنشاء مستودع نموذج على Hugging Face Hub! يمكننا استخدام مكتبة 🤗 Hub لتوليد الاسم الكامل لمستودعنا أولاً:

```python
from huggingface_hub import get_full_repo_name

model_name = "distilbert-base-uncased-finetuned-imdb-accelerate"
repo_name = get_full_repo_name(model_name)
repo_name
```

```python out
'lewtun/distilbert-base-uncased-finetuned-imdb-accelerate'
```

ثم إنشاء واستنساخ المستودع باستخدام فئة `Repository` من 🤗 Hub:

```python
from huggingface_hub import Repository

output_dir = model_name
repo = Repository(output_dir, clone_from=repo_name)
```

بعد القيام بذلك، الأمر بسيط جدًا، حيث نكتب حلقة التدريب والتقييم الكاملة:

```python
from tqdm.auto import tqdm
import torch
import math

progress_bar = tqdm(range(num_training_steps))

for epoch in range(num_train_epochs):
    # Training
    model.train()
    for batch in train_dataloader:
        outputs = model(**batch)
        loss = outputs.loss
        accelerator.backward(loss)

        optimizer.step()
        lr_scheduler.step()
        optimizer.zero_grad()
        progress_bar.update(1)

    # Evaluation
    model.eval()
    losses = []
    for step, batch in enumerate(eval_dataloader):
        with torch.no_grad():
            outputs = model(**batch)

        loss = outputs.loss
        losses.append(accelerator.gather(loss.repeat(batch_size)))

    losses = torch.cat(losses)
    losses = losses[: len(eval_dataset)]
    try:
        perplexity = math.exp(torch.mean(losses))
    except OverflowError:
        perplexity = float("inf")

    print(f">>> Epoch {epoch}: Perplexity: {perplexity}")

    # Save and upload
    accelerator.wait_for_everyone()
    unwrapped_model = accelerator.unwrap_model(model)
    unwrapped_model.save_pretrained(output_dir, save_function=accelerator.save)
    if accelerator.is_main_process:
        tokenizer.save_pretrained(output_dir)
        repo.push_to_hub(
            commit_message=f"Training in progress epoch {epoch}", blocking=False
        )
```

```python out
>>> Epoch 0: Perplexity: 11.397545307900472
>>> Epoch 1: Perplexity: 10.904909330983092
>>> Epoch 2: Perplexity: 10.729503505340409
```

رائع، تمكنا من تقييم الارتباك مع كل حقبة والتأكد من أن عمليات التدريب المتعددة قابلة للتكرار!

## استخدام نموذجنا المُدرب جيدًا

يمكنك التفاعل مع نموذجك المُدرب جيدًا إما باستخدام أداته على Hub أو محليًا باستخدام `pipeline` من 🤗 Transformers. دعنا نستخدم الأخير لتنزيل نموذجنا باستخدام خط أنابيب `fill-mask`:

```python
from transformers import pipeline

mask_filler = pipeline(
    "fill-mask", model="huggingface-course/distilbert-base-uncased-finetuned-imdb"
)
```

بعد ذلك، يمكننا إطعام خط الأنابيب نصنا العينة "This is a great [MASK]" ورؤية أفضل 5 تنبؤات:

```python
preds = mask_filler(text)

for pred in preds:
    print(f">>> {pred['sequence']}")
```

```python out
'>>> this is a great movie.'
'>>> this is a great film.'
'>>> this is a great story.'
'>>> this is a great movies.'
'>>> this is a great character.'
```

ممتاز -- من الواضح أن نموذجنا قد تكيف مع أوزانه للتنبؤ بالكلمات التي ترتبط ارتباطًا وثيقًا بالأفلام!

<Youtube id="0Oxphw4Q9fo"/>

هذا يختتم تجربتنا الأولى في تدريب نموذج اللغة. في [القسم 6](/course/en/chapter7/6) ستتعلم كيفية تدريب نموذج ذاتي الانحدار مثل GPT-2 من الصفر؛ انتقل إلى هناك إذا كنت ترغب في معرفة كيفية تدريب نموذج Transformer الخاص بك!

<Tip>

✏️ **جربه!** لقياس فوائد تكيف المجال، قم بتدريب مصنف على تسميات IMDb لكل من نقاط التحقق المُدربة مسبقًا والمُدربة جيدًا لـ DistilBERT. إذا كنت بحاجة إلى مراجعة حول التصنيف النصي، راجع [الفصل 3](/course/chapter3).

</Tip>