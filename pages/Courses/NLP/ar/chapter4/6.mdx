# اختبار نهاية الفصل

لاختبار ما تعلمته في هذا الفصل!

### 1. ما هي القيود المفروضة على النماذج في المنصة؟

**الإجابة:** لا توجد قيود على النماذج التي يتم تحميلها على المنصة.

### 2. كيف يمكنك إدارة النماذج على المنصة؟

**الإجابة:** يمكن إدارة النماذج على المنصة باستخدام Git وgit-lfs.

### 3. ما الذي يمكنك فعله باستخدام الواجهة الرسومية للمنصة؟

**الإجابة:** يمكنك إنشاء مستودع نموذج جديد، وإدارة الملفات وتحريرها، وتحميل الملفات، وعرض الاختلافات بين الإصدارات.

### 4. ما هي بطاقة النموذج (Model Card)؟

**الإجابة:** بطاقة النموذج هي طريقة لضمان إمكانية إعادة الاستخدام والعدالة. فهي توفر المعلومات الصحيحة التي تساعد المستخدمين على الاستفادة من النموذج وفهم حدوده وتحيزاته.

### 5. أي من هذه الكائنات في مكتبة 🤗 Transformers يمكن مشاركتها مباشرة على المنصة باستخدام `push_to_hub()`؟

**الإجابة:** يمكن مشاركة النماذج، ومكونات النماذج، والمحللات مباشرة باستخدام `push_to_hub()`. وفي حالة PyTorch، يمكن أيضًا مشاركة المدرب باستخدام نفس الطريقة.

### 6. ما هي أول خطوة عند استخدام طريقة `push_to_hub()` أو أدوات واجهة سطر الأوامر (CLI)؟

**الإجابة:** أول خطوة هي تسجيل الدخول باستخدام واجهة سطر الأوامر عن طريق تشغيل "huggingface-cli login" في المحطة الطرفية أو "notebook_login()" في الدفتر.

### 7. إذا كنت تستخدم نموذجًا ومحللًا، فكيف يمكنك تحميلهما إلى المنصة؟

**الإجابة:** يمكنك تحميلهما عن طريق استدعاء طريقة `push_to_hub` مباشرة على النموذج والمحلل.

### 8. ما هي عمليات Git التي يمكنك تنفيذها باستخدام فئة `Repository`؟

**الإجابة:** يمكنك تنفيذ عمليات الالتزام (commit) والسحب (pull) والدفع (push) باستخدام طرق `git_commit` و`git_pull` و`git_push` على التوالي.