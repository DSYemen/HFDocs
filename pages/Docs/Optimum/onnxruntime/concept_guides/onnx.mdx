# ONNX 🤝 ONNX Runtime

ONNX هو معيار مفتوح يحدد مجموعة مشتركة من المشغلات وصيغة ملف مشتركة لتمثيل نماذج التعلم العميق في مجموعة واسعة من الأطر، بما في ذلك PyTorch وTensorFlow. عندما يتم تصدير نموذج إلى تنسيق ONNX، يتم استخدام هذه المشغلات لبناء رسم بياني حسابي (يُطلق عليه غالبًا تمثيل وسيط) يمثل تدفق البيانات عبر الشبكة العصبية.

<Tip>
يمكنك استخدام [Netron](https://netron.app/) لتصور أي ملف ONNX على Hugging Face Hub. ما عليك سوى إضافة عنوان URL للملف إلى `http://netron.app?url=` كما في [هذا المثال](https://netron.app/?url=https://huggingface.co/cmarkea/distilcamembert-base-ner/blob/main/model.onnx)
</Tip>

من خلال عرض رسم بياني بمشغلين وأنواع بيانات موحدة، تجعل ONNX من السهل التبديل بين الأطر. على سبيل المثال، يمكن تصدير نموذج مدرب في PyTorch إلى تنسيق ONNX ثم استيراده في TensorFlow (والعكس صحيح).

يبرز ONNX حقًا عندما يتم اقترانها بمسرع مخصص مثل ONNX Runtime، أو ORT اختصارًا. يوفر ORT أدوات لتحسين رسم ONNX من خلال تقنيات مثل دمج المشغل والطي الثابت، ويحدد واجهة لمقدمي التنفيذ تسمح لك بتشغيل النموذج على أنواع مختلفة من الأجهزة.